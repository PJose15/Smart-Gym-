---
phase: 03-mobile-challenges
plan: 05
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified: []
autonomous: false
requirements: [CHAL-01, CHAL-02, CHAL-03, CHAL-04]

must_haves:
  truths:
    - "Both app test suites and both tsc checks pass with all Phase 3 code merged"
    - "A real member on a device browses the 6 seeded Iron Society challenges, joins one, feels the haptic, sees confetti, and finds their pinned rank on a leaderboard"
    - "A stale deep link to a nonexistent challenge shows the graceful state on device"
  artifacts:
    - path: ".planning/phases/03-mobile-challenges/03-05-SUMMARY.md"
      provides: "Gate results + human verification record"
  key_links:
    - from: "demo environment (Iron Society seed)"
      to: "mobile challenges tab"
      via: "on-device walkthrough against seeded gym_challenges data"
---

<objective>
Final phase gate: prove the whole Phase 3 slice works — automated suites green across both apps, then a human on-device walkthrough of every CHAL requirement against the Iron Society demo environment.

Purpose: Haptics, confetti feel, tab navigation, and leaderboard visuals cannot be verified by jest — this is the one consolidated human checkpoint for the phase (per checkpoint anti-pattern guidance: one verification at the end, not per-plan).
Output: Green automated gate + human sign-off recorded in SUMMARY.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mobile-challenges/03-VALIDATION.md
@.planning/phases/03-mobile-challenges/03-03-SUMMARY.md
@.planning/phases/03-mobile-challenges/03-04-SUMMARY.md
@DEMO_SETUP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated gate — both apps</name>
  <files>none (verification only)</files>
  <action>
Run the complete Phase 3 gate:

1. `cd apps/mobile && npx tsc --noEmit && npx jest --silent` — expect 135 baseline + all new challenge tests green
2. `cd apps/web-admin && npx tsc --noEmit && npx jest --silent` — expect 342+ tests green (incl. verifyMember suite)
3. Confirm migration 028 was applied (03-01 checkpoint record in 03-01-SUMMARY.md says "applied")
4. Grep sanity: no hardcoded hex colors in new challenge files — `grep -En "#[0-9a-fA-F]{3,6}" apps/mobile/src/components/challenges/ apps/mobile/app/challenges/ apps/mobile/app/\(tabs\)/challenges.tsx -r` should return only token-file imports (expected: zero matches in these files)

If anything fails, fix forward within this plan before the checkpoint.
  </action>
  <verify>
    <automated>bash -c "cd apps/mobile && npx tsc --noEmit && npx jest --silent && cd ../web-admin && npx tsc --noEmit && npx jest --silent"</automated>
  </verify>
  <done>Both suites green, both tsc clean, no ad-hoc colors in Phase 3 files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device walkthrough — Iron Society demo</name>
  <files>none (human verification only)</files>
  <action>
Present the walkthrough checklist below to the user and pause. All automation is already complete (Task 1 gate green); this checkpoint only confirms feel/visual behaviors jest cannot test (haptics, confetti, tab navigation, leaderboard visuals).
  </action>
  <what-built>Complete mobile challenges feature: Challenges tab (browse active/completed with countdowns), one-tap join with haptic + confetti, personal progress bars, leaderboard with pinned YOU row, graceful deleted/ended states. Demo env: Iron Society gym, 50 members, 6 seeded challenges (5 active + 1 completed).</what-built>
  <how-to-verify>
    Using a physical device (haptics don't fire in simulator) signed in as a demo Iron Society member (see DEMO_SETUP.md). Ensure apps/mobile/.env has EXPO_PUBLIC_API_URL pointing at a running web-admin (LAN IP for physical device, e.g. http://192.168.x.x:3000).

    1. CHAL-01 — Open the new Challenges tab (trophy icon). Expect 5 cards under Active with type badges, descriptions, and "Xd left" countdowns; toggle to Completed and see 1 ended challenge
    2. CHAL-02 — Open an active challenge you haven't joined, tap Join. Expect: button disables, success haptic buzz, confetti burst, card flips to Joined state, your row appears on the leaderboard (pull-to-refresh if needed — within a minute)
    3. CHAL-02 — Re-open the same challenge: Join CTA gone/disabled (already joined). Open the completed challenge: no Join CTA, "Challenge ended" banner
    4. CHAL-03 — On a joined challenge with scores, confirm the progress bar reads "You: X · Leader: Y" (and respects your kg/lbs setting for volume/PR challenges)
    5. CHAL-04 — On a challenge where you're outside the top 10, confirm top ranks show gold/silver/bronze and your "YOU" row is pinned below a divider with your real rank
    6. CHAL-04 — Navigate manually to a bogus id (e.g. deep link `nexera://challenges/00000000-0000-0000-0000-000000000000` or edit the route): expect "This challenge is no longer available" with a working back button — no crash
  </how-to-verify>
  <verify>
    <automated>none — human-only checkpoint (all automated coverage ran in Task 1)</automated>
  </verify>
  <done>User replied "approved" confirming all 6 device checks pass (browse, join+haptic+confetti, joined/ended CTA states, progress bar with unit, pinned YOU row, stale deep-link graceful state).</done>
  <resume-signal>Type "approved" if all 6 checks pass, or describe which check failed and what you saw</resume-signal>
</task>

</tasks>

<verification>
- Automated gate green (both apps, tsc + jest)
- All 6 device checks approved by human
</verification>

<success_criteria>
- Every Phase 3 success criterion from ROADMAP.md verified TRUE on a real device against demo data
- Phase ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-challenges/03-05-SUMMARY.md`
</output>
