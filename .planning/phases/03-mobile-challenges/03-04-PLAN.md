---
phase: 03-mobile-challenges
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - apps/mobile/src/components/challenges/ChallengeLeaderboard.tsx
  - apps/mobile/app/challenges/[id].tsx
  - apps/mobile/app/_layout.tsx
autonomous: true
requirements: [CHAL-02, CHAL-03, CHAL-04]

must_haves:
  truths:
    - "Member joins an active challenge with one tap and immediately sees joined state (optimistic), a success haptic, and confetti"
    - "Join CTA is disabled/absent when already joined, when the challenge has ended, or when entry_mode is invite-only"
    - "Member sees 'You: X · Leader: Y' progress bar on the detail screen after joining"
    - "Leaderboard shows top ranks with gold/silver/bronze styling and pins the member's own 'YOU' row below a divider when they are outside the top 10"
    - "Opening a deleted challenge id shows 'This challenge is no longer available' with back navigation — never a crash; an ended challenge shows an ended state"
  artifacts:
    - path: "apps/mobile/src/components/challenges/ChallengeLeaderboard.tsx"
      provides: "Ranked list with medal colors + pinned YOU row"
      min_lines: 80
    - path: "apps/mobile/app/challenges/[id].tsx"
      provides: "Challenge detail screen: header, join flow, progress, leaderboard, not-found/ended states"
      min_lines: 150
  key_links:
    - from: "apps/mobile/app/challenges/[id].tsx"
      to: "apps/mobile/src/lib/challengeService.ts"
      via: "fetchChallengeDetail (null -> not-found state) + joinChallenge (Bearer API call)"
      pattern: "fetchChallengeDetail|joinChallenge"
    - from: "apps/mobile/app/challenges/[id].tsx"
      to: "expo-haptics + ConfettiEffect"
      via: "Haptics.notificationAsync(Success) + confetti on 'joined' result, gated by useReducedMotion"
      pattern: "notificationAsync"
    - from: "apps/mobile/src/components/challenges/ChallengeLeaderboard.tsx"
      to: "apps/mobile/src/lib/challengeLogic.ts"
      via: "buildLeaderboardRows for top-N + pinned me"
      pattern: "buildLeaderboardRows"
    - from: "apps/mobile/app/_layout.tsx"
      to: "challenges/[id] stack registration"
      via: "Stack.Screen entry (deep-link target)"
      pattern: "challenges/\\[id\\]"
---

<objective>
Build the challenge detail experience: join with one tap (CHAL-02), personal progress (CHAL-03), and the leaderboard with an always-visible own rank plus graceful deleted/ended handling (CHAL-04).

Purpose: This screen is where competition actually happens — join, track, compare. It is also the deep-link target for Phase 6 rank-change pushes, so the stale-link states must be bulletproof.
Output: `challenges/[id]` route with full state handling, ChallengeLeaderboard component, join flow with haptic + confetti celebration.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mobile-challenges/03-RESEARCH.md
@.planning/phases/03-mobile-challenges/03-02-SUMMARY.md
@apps/mobile/app/_layout.tsx
@apps/mobile/src/components/effects/ConfettiEffect.tsx
@apps/mobile/src/theme/colors.ts
@apps/mobile/src/lib/challengeService.ts
@apps/mobile/src/lib/challengeLogic.ts
@apps/web-admin/src/app/(member)/gym/challenges/[challengeId]/page.tsx
</context>

<interfaces>
<!-- Contracts from Wave 1 — consume directly -->

From apps/mobile/src/lib/challengeService.ts (plan 03-02):
```typescript
export async function fetchChallengeDetail(challengeId: string, memberId: string): Promise<ChallengeDetail | null>;
// null = deleted/missing challenge (stale deep link) — render "no longer available"
export async function joinChallenge(challengeId: string, memberId: string, gymId: string):
  Promise<'joined' | 'already_joined' | 'ended' | 'error'>;
// cache key: `challenge:${challengeId}` with CacheTTL.challengeDetail (2 min)
```

From apps/mobile/src/lib/challengeLogic.ts (plan 03-02):
```typescript
export function buildLeaderboardRows(participants: ChallengeParticipant[], myMemberId: string, topN?: number):
  { top: ChallengeParticipant[]; pinnedMe: ChallengeParticipant | null };
export function canJoin(detail: ChallengeDetail): boolean;
export function progressPct(myScore: number | null, topScore: number): number;
export function formatScore(score: number, type: ChallengeType, unit: WeightUnit): string;
export function countdownLabel(endDate: string, now?: Date): string;
export function challengeIcon(type: string): string;
```

ChallengeDetail adds over ChallengeListItem: entry_mode ('open'|'opt-in'|'invite'), prize_type, prize_description, participants (ChallengeParticipant[] with display_name/avatar_url resolved, ordered by current_rank), my_participation.

Theme tokens (src/theme/colors.ts): colors.gold, colors.silver, colors.bronze (rank medals), colors.info + colors.infoSubtle ("You" highlight), colors.primary/primarySubtle, colors.success/successSubtle.

Bearer auth + RLS: plan 03-01 delivered verifyMember Bearer support (join returns 201 for mobile) and Migration 028 (participants readable by members). At runtime both are prerequisites; this plan's code assumes they exist.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ChallengeLeaderboard component with medals + pinned YOU row</name>
  <files>apps/mobile/src/components/challenges/ChallengeLeaderboard.tsx</files>
  <action>
Create `apps/mobile/src/components/challenges/ChallengeLeaderboard.tsx`. Props: `{ participants: ChallengeParticipant[]; myMemberId: string; challengeType: ChallengeType; weightUnit: WeightUnit }`.

- Use `buildLeaderboardRows(participants, myMemberId, 10)` — never slice/filter inline
- Each row: rank indicator, avatar (initial-letter circle fallback when avatar_url null — match feed avatar handling), display_name (+ " (You)" suffix when mine), score via `formatScore(current_score, challengeType, weightUnit)`
- Rank 1/2/3: medal-colored rank badge using `colors.gold` / `colors.silver` / `colors.bronze` (adapted from web RANK_MEDALS pattern); ranks 4+: plain `#N` in textSecondary
- My row (in top or pinned): `colors.infoSubtle` background + `colors.info` accent border
- When `pinnedMe` is non-null: render a divider row (1px `colors.border` line with a "···" or "Your rank" micro-label) then the pinned row showing my actual `current_rank`
- Empty participants: "No participants yet — be the first to join"
- Rows get `accessibilityLabel` like "Rank 4, Maria, 12,300 pounds"
- StyleSheet.create, tokens only; render inside parent ScrollView as a plain View list (no nested FlatList/VirtualizedList warning)
  </action>
  <verify>
    <automated>bash -c "cd apps/mobile && npx tsc --noEmit && grep -q 'buildLeaderboardRows' src/components/challenges/ChallengeLeaderboard.tsx && grep -Eq 'colors\.gold' src/components/challenges/ChallengeLeaderboard.tsx"</automated>
  </verify>
  <done>Component compiles, uses buildLeaderboardRows + medal tokens, handles empty list and pinned row.</done>
</task>

<task type="auto">
  <name>Task 2: Challenge detail screen with join flow + graceful states, register route</name>
  <files>apps/mobile/app/challenges/[id].tsx, apps/mobile/app/_layout.tsx</files>
  <action>
Create `apps/mobile/app/challenges/[id].tsx` (new `app/challenges/` directory):

1. `useLocalSearchParams()` for id; validate it looks like a UUID — invalid id goes straight to the not-found state (stale deep links may carry garbage)
2. Context: `fetchFeedContext()` for memberId/gymId; then `cacheFirst` key `challenge:${id}` (TTL `challengeDetail`) around `fetchChallengeDetail(id, memberId)`
3. Screen states (explicit, mutually exclusive):
   - loading -> skeleton (SkeletonBone-based header + rows; reuse ChallengesScreenSkeleton pieces or inline bones)
   - `detail === null` -> "This challenge is no longer available" full-screen state with trophy-off emoji/icon + "Back to challenges" button (`router.back()` with fallback `router.replace('/(tabs)/challenges')`). NEVER render undefined data (research Pitfall 5)
   - `detail.is_active === false` -> render full detail + leaderboard but with an "Challenge ended" banner (textSecondary chip) and NO join CTA
   - active -> full detail
4. Header section: `challengeIcon` + type badge, title, description, countdown chip (`countdownLabel`), prize row when prize_description present, "{total_participants} competing"
5. Join CTA: visible only when `canJoin(detail)`. On press:
   - set local joining state (disable button, spinner)
   - `const result = await joinChallenge(id, memberId, gymId)`
   - 'joined': optimistic update (is_joined=true, my_participation appended with rank = participants.length+1, score 0), `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)`, fire `ConfettiEffect` (import from `src/components/effects/ConfettiEffect`, match its existing trigger API) — skip confetti when `useReducedMotion` reports reduced; then invalidate cache + background re-fetch detail so the real leaderboard row appears
   - 'already_joined': set is_joined=true silently (no error — state was stale)
   - 'ended': show ended banner state
   - 'error': inline error toast/text "Couldn't join — try again"
   - entry_mode 'invite': CTA replaced by "Invite-only challenge" label
6. Progress section (when is_joined && top_score > 0): bar + "You: {formatScore(my_score)} · Leader: {formatScore(top_score)}" — same visual language as ChallengeCard (CHAL-03)
7. Leaderboard section: `<ChallengeLeaderboard participants={detail.participants} myMemberId={memberId} challengeType={detail.challenge_type} weightUnit={unit} />`
8. Pull-to-refresh re-fetches detail bypassing cache
9. StyleSheet.create + tokens; accessibility labels on CTA and back button

Register the route in `apps/mobile/app/_layout.tsx` — add to the Stack alongside existing entries:
```tsx
<Stack.Screen name="challenges/[id]" options={{ title: 'Challenge', headerShown: true }} />
```
(match header styling options used by sibling detail screens, e.g. coach-notes/[id]).

Run full mobile suite to confirm no regression.
  </action>
  <verify>
    <automated>bash -c "cd apps/mobile && npx tsc --noEmit && npx jest --silent && grep -q 'no longer available' app/challenges/\[id\].tsx && grep -q 'notificationAsync' app/challenges/\[id\].tsx && grep -q 'challenges/\[id\]' app/_layout.tsx"</automated>
  </verify>
  <done>Detail screen handles all 4 states (loading/not-found/ended/active), join flow with haptic + reduced-motion-gated confetti, route registered; full mobile suite green, tsc clean.</done>
</task>

</tasks>

<verification>
- Detail screen renders header, progress, leaderboard from ChallengeDetail
- Join: one tap -> optimistic joined state + haptic + confetti; disabled when joined/ended/invite
- Leaderboard: medals for top 3, pinned YOU row when outside top 10
- Stale deep link (bad/deleted id) -> "no longer available" state; ended -> ended banner, no CTA
- Full mobile suite green, tsc clean
</verification>

<success_criteria>
- CHAL-02: one-tap join with optimistic update + success haptic, correctly disabled states
- CHAL-03: detail-level "You vs Leader" progress bar
- CHAL-04: own rank always visible (pinned row) + graceful deleted/ended handling, no crash paths
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-challenges/03-04-SUMMARY.md`
</output>
