---
phase: 05-uptimizeai-agent-connection
plan: 06
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - apps/web-admin/src/lib/agents/atRiskScan.ts
  - apps/web-admin/src/lib/agents/__tests__/atRiskScan.test.ts
  - apps/web-admin/src/app/api/owner/at-risk/route.ts
  - apps/web-admin/src/app/api/owner/at-risk/__tests__/at-risk.test.ts
  - apps/web-admin/src/app/api/cron/agent-weekly/route.ts
  - apps/web-admin/src/app/api/cron/agent-weekly/__tests__/agent-weekly.test.ts
autonomous: true
requirements: [AGENT-03, AGENT-04]
must_haves:
  truths:
    - "Opening the owner at-risk dashboard fires retention-agent 'member-at-risk' once PER at-risk member (never one blanket per page load), 7-day member cooldown absorbing repeat visits"
    - "The weekly cron fires operations-agent 'weekly-summary' per gym with active members, and never for zero-member gyms"
    - "The weekly cron independently scans every gym for at-risk members (early warning) — dedup makes owner-visit and cron fires idempotent per member per 7 days"
    - "At-risk detection logic lives in ONE shared module used by both the route and the cron"
  artifacts:
    - path: "apps/web-admin/src/lib/agents/atRiskScan.ts"
      provides: "fetchGymAtRiskMembers(admin, gymId) shared scan helper"
      exports: ["fetchGymAtRiskMembers"]
    - path: "apps/web-admin/src/app/api/owner/at-risk/route.ts"
      provides: "per-member retention-agent call site on at-risk detection"
      contains: "member-at-risk"
    - path: "apps/web-admin/src/app/api/cron/agent-weekly/route.ts"
      provides: "weekly-summary + member-at-risk early-warning scans"
      exports: ["POST"]
  key_links:
    - from: "apps/web-admin/src/app/api/owner/at-risk/route.ts"
      to: "apps/web-admin/src/lib/agents/atRiskScan.ts"
      via: "import { fetchGymAtRiskMembers }"
      pattern: "fetchGymAtRiskMembers"
    - from: "apps/web-admin/src/app/api/cron/agent-weekly/route.ts"
      to: "apps/web-admin/src/lib/agents/atRiskScan.ts"
      via: "same shared helper per gym"
      pattern: "fetchGymAtRiskMembers"
    - from: "apps/web-admin/src/app/api/cron/agent-weekly/route.ts"
      to: "@/lib/billing/triggerAgent"
      via: "batched fire-and-forget triggers"
      pattern: "triggerUptimizeAIAgent"
---

<objective>
Complete the retention/operations pipeline: automation #2 (member-at-risk from the owner at-risk route — AGENT-03) and the weekly cron route covering automations #9 (weekly-summary) and the at-risk early-warning scan (AGENT-04's "at-risk early warning").

Purpose: these three share the at-risk detection logic, so they live in one plan around a single extracted helper (`lib/agents/atRiskScan.ts`) — avoiding file conflicts with sibling Wave 2 plans and logic duplication between route and cron.

Output: shared scan helper, wired at-risk route (Pitfall 4: per-member, never per-request), agent-weekly cron route. The pg_cron schedule for agent-weekly already exists (migration 029, plan 05-01).
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-uptimizeai-agent-connection/05-RESEARCH.md
@.planning/phases/05-uptimizeai-agent-connection/05-02-SUMMARY.md
@apps/web-admin/src/app/api/owner/at-risk/route.ts
@apps/web-admin/src/app/api/cron/dna-recompute/route.ts
@apps/web-admin/src/lib/billing/triggerAgent.ts
</context>

<interfaces>
```typescript
// @nexera/ai-assist (existing, pure)
export function computeAtRiskMembers(members: MemberData[]): AtRiskMember[]
export interface AtRiskMember { profileId: string; memberName: string; reasons: AtRiskReason[] }
export interface MemberData { profileId: string; memberName: string; lastWorkoutAt: string | null;
  discomfortCount7d: number; discomfortBodyAreas: string[]; plateauExercises: string[] }
```

Current at-risk route logic to extract (read 2026-07-19): fetch active members (id, display_name) for gym + workout_sessions (member_id, created_at) for last 30 days ordered desc → build last-workout map (first occurrence = most recent) → map to MemberData with zeros for discomfort/plateau → `computeAtRiskMembers(memberData)`. Route returns the AtRiskMember[] as JSON — response shape MUST NOT change (owner dashboard consumes it).

Cron auth: dual-header pattern from dna-recompute (x-smartgym-internal-key OR Authorization Bearer). Batch pattern: batches of 10 with Promise.allSettled.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract atRiskScan helper + wire per-member retention triggers into the at-risk route (RED then GREEN)</name>
  <files>apps/web-admin/src/lib/agents/atRiskScan.ts, apps/web-admin/src/lib/agents/__tests__/atRiskScan.test.ts, apps/web-admin/src/app/api/owner/at-risk/route.ts, apps/web-admin/src/app/api/owner/at-risk/__tests__/at-risk.test.ts</files>
  <behavior>
RED first — two test files:

atRiskScan.test.ts (mock the admin client passed in as a param — chainable builder):
- fetchGymAtRiskMembers(admin, gymId) queries active members + last-30d sessions for that gym and returns computeAtRiskMembers output (feed canned data producing 1 at-risk + 1 healthy member; assert only the at-risk one returns)
- Empty gym → []

at-risk.test.ts (mock `@/lib/auth/verifyStaff`, `@/lib/agents/atRiskScan`, `@/lib/billing/triggerAgent`):
- 2 at-risk members returned by helper → triggerUptimizeAIAgent called twice: ('retention-agent', { event: 'member-at-risk', gym_id, member_id: <profileId>, is_agent_initiated: false }) — member_id NEVER null (Pitfall 4)
- 0 at-risk → no agent calls
- Response body is exactly the AtRiskMember[] array (unchanged contract)
- Trigger rejection does not affect the 200 response
  </behavior>
  <action>
1. Create `apps/web-admin/src/lib/agents/atRiskScan.ts`: export `async function fetchGymAtRiskMembers(admin: SupabaseClient, gymId: string): Promise<AtRiskMember[]>` — move the members+sessions queries and MemberData mapping verbatim from the at-risk route (keep the 30-day bound + explicit selects). Import computeAtRiskMembers + types from '@nexera/ai-assist'.
2. Rewire `owner/at-risk/route.ts` to call the helper, then per at-risk member fire retention-agent 'member-at-risk' (fire-and-forget, .catch(console.error), payload: gym_id, member_id: m.profileId, is_agent_initiated: false). The 7-day per-(gym, agent, event, member) cooldown in the trigger route absorbs repeated dashboard visits. Return the same JSON as before.
3. RED → GREEN → commits (test(05-06): / feat(05-06): with Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>).
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="atRiskScan|at-risk" --silent && npx tsc --noEmit</automated>
  </verify>
  <done>Shared helper extracted; at-risk route fires per-member with member_id set; dashboard response contract unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: agent-weekly cron — weekly-summary + at-risk early warning (RED then GREEN)</name>
  <files>apps/web-admin/src/app/api/cron/agent-weekly/route.ts, apps/web-admin/src/app/api/cron/agent-weekly/__tests__/agent-weekly.test.ts</files>
  <behavior>
RED first — agent-weekly.test.ts (mock `@supabase/supabase-js` createClient, `@/lib/agents/atRiskScan`, `@/lib/billing/triggerAgent`; INTERNAL_WEBHOOK_KEY in env). Tests:
- Auth: 401 without/with wrong key; 200 with either header form
- Gym with 12 active members + 30 sessions last 7d → operations-agent fired with ('operations-agent', { event: 'weekly-summary', gym_id, active_members: 12, sessions_7d: 30, is_agent_initiated: false })
- Gym with 0 active members → NO weekly-summary fire (suppressed)
- fetchGymAtRiskMembers returns 2 at-risk for a gym → retention-agent 'member-at-risk' fired per member with member_id
- Response counters: { summaries_triggered, at_risk_triggered }
- One gym's scan throwing does not abort remaining gyms (allSettled)
  </behavior>
  <action>
Create `apps/web-admin/src/app/api/cron/agent-weekly/route.ts`:
- `export const dynamic = 'force-dynamic'`; POST; dual-header auth; inline getAdminClient().
- Fetch gyms: `gyms.select('id, name').neq('subscription_status', 'cancelled')` (explicit selects — repo convention).
- Per gym (process gyms in batches of 10, Promise.allSettled):
  a. Count active members (`members`, head:true count, eq gym_id + status 'active'). If 0 → skip gym entirely.
  b. Count completed sessions last 7d (`workout_sessions`, head:true count, eq gym_id, gte created_at 7d ago).
  c. Fire operations-agent 'weekly-summary' with { gym_id, active_members, sessions_7d, is_agent_initiated: false }. The 6-day cooldown (05-02 map) makes Sunday reruns idempotent while never blocking next week.
  d. Early warning: `fetchGymAtRiskMembers(admin, gym.id)` → per at-risk member fire retention-agent 'member-at-risk' (same payload contract as Task 1 — cooldown dedups against owner-route fires within 7 days).
- Return `{ summaries_triggered, at_risk_triggered, gyms_scanned }`.
- Tier note: starter gyms' fires get tier-skipped by the trigger route and logged 'skipped' — correct and observable (AGENT-05); the cron does not pre-filter by tier.
RED → GREEN → commits.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="agent-weekly" --silent && npx jest --silent && npx tsc --noEmit</automated>
  </verify>
  <done>Weekly cron fires weekly-summary per active gym + member-at-risk early warnings via the shared helper; zero-member gyms suppressed; full web-admin suite green, tsc clean.</done>
</task>

</tasks>

<verification>
- atRiskScan + at-risk + agent-weekly tests green; full suite green; tsc clean
- grep confirms both route and cron import fetchGymAtRiskMembers (single source of truth)
</verification>

<success_criteria>
- Automation #2 (member-at-risk, owner route) + #9 (weekly-summary) + the weekly at-risk early-warning scan wired; AGENT-04's five scheduled scans now fully covered together with plan 05-05
</success_criteria>

<output>
After completion, create `.planning/phases/05-uptimizeai-agent-connection/05-06-SUMMARY.md`
</output>
