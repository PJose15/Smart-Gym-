---
phase: 05-uptimizeai-agent-connection
plan: 07
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06"]
files_modified:
  - .planning/phases/05-uptimizeai-agent-connection/05-VALIDATION.md
autonomous: false
requirements: [AGENT-05]
must_haves:
  truths:
    - "Full web-admin suite green (347 baseline + all Phase 5 tests) and tsc clean in both apps; mobile suite untouched and still green"
    - "All 13 automations demonstrably fire from their real sources in staging against the Iron Society demo gym, confirmed via echo receiver + smartgym_agent_logs"
    - "Tier matrix holds: starter = 0 gym-scoped fires (all 'skipped'), growth = retention+engagement set only, pro = all 5 agent families; the 2 PLATFORM_EVENTS fire regardless of tier (human-approved design)"
    - "Cooldown provably blocks an immediate duplicate fire (second attempt logs 'skipped')"
  artifacts:
    - path: ".planning/phases/05-uptimizeai-agent-connection/05-VALIDATION.md"
      provides: "Signed-off validation contract (nyquist_compliant: true, walkthrough results recorded)"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "echo receiver console + smartgym_agent_logs.action_taken"
      to: "each of the 13 automations"
      via: "staged trigger-source walkthrough"
      pattern: "echo-received"
---

<objective>
Phase 5 gate: run the full automated gate, then a human staging walkthrough proving all 13 automations fire end-to-end from their REAL sources (not direct trigger-route curls, except the 3 Stripe ones) against the Iron Society demo environment with the echo receiver as the UptimizeAI stand-in.

Purpose: AGENT-05 — every fire observable; tier gating exact; the phase's success criteria verified before /gsd:verify-work. Staging-ready per LOCKED decision: verification target is the dev echo receiver; going live later = set UPTIMIZE_WEBHOOK_URL.

Human decision surfaced here for approval: the PLATFORM_EVENTS tier-gating bypass for 'new-gym-onboarded' and 'upgrade-opportunity' (rationale in 05-02 objective — without it both automations are permanently dead code because their firing gyms never have Pro).
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-uptimizeai-agent-connection/05-VALIDATION.md
@.planning/phases/05-uptimizeai-agent-connection/05-RESEARCH.md
@.planning/phases/05-uptimizeai-agent-connection/05-01-SUMMARY.md
@.planning/phases/05-uptimizeai-agent-connection/05-02-SUMMARY.md
@DEMO_SETUP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated phase gate</name>
  <files>.planning/phases/05-uptimizeai-agent-connection/05-VALIDATION.md</files>
  <action>
1. Run the full gates:
   - `cd apps/web-admin && npx tsc --noEmit && npx jest --silent` (expect 347 baseline + all new Phase 5 suites: agents/trigger, complete-agents, challenges complete, featureGate-upgrade, register additions, agent-daily, agent-weekly, atRiskScan, at-risk)
   - `cd apps/mobile && npx tsc --noEmit && npx jest --silent` (untouched — must still be 227/227)
   - `pnpm -F @nexera/ai-assist test -- --run` (untouched — 399)
2. Verify wiring greps (each must return matches):
   - `grep -r "triggerUptimizeAIAgent" apps/web-admin/src/app/api/sessions apps/web-admin/src/app/api/challenges apps/web-admin/src/app/api/owner/at-risk apps/web-admin/src/app/api/onboard/register apps/web-admin/src/app/api/cron/agent-daily apps/web-admin/src/app/api/cron/agent-weekly`
   - `grep "UPTIMIZE_WEBHOOK_URL" apps/web-admin/src/app/api/agents/trigger/route.ts apps/web-admin/.env.example`
3. Update 05-VALIDATION.md: fill the Per-Task Verification Map rows with actual test file paths + PASS status per requirement (AGENT-01..05 → the map in 05-RESEARCH.md Validation Architecture), check off Wave 0 requirements and Sign-Off items, set frontmatter `wave_0_complete: true` (nyquist_compliant already true).
4. Commit: `docs(05-07): phase 5 automated gate green + validation sign-off` with Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx tsc --noEmit && npx jest --silent</automated>
  </verify>
  <done>All three suites green, tsc clean in both apps, wiring greps confirm every call site, VALIDATION.md signed off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Staging walkthrough — all 13 automations vs Iron Society</name>
  <files>none (verification only)</files>
  <action>
Human verification step. Claude has already automated everything (Task 1 gate green); the user now runs the staged walkthrough below against the local dev server + Iron Society demo gym and approves or reports failures. Claude pauses until the resume signal.
  </action>
  <what-built>The complete Phase 5 agent pipeline: hardened trigger route (cooldown dedup, loop-safety flag, UPTIMIZE forwarding with failure recording), 7 event-driven call sites, 2 cron scan routes, echo receiver, migration 029.</what-built>
  <how-to-verify>
Setup (once): in apps/web-admin/.env.local set `UPTIMIZE_WEBHOOK_URL=http://localhost:3000/api/dev/agent-echo`, `UPTIMIZE_API_KEY=staging-key`, `DEMO_ECHO_AGENTS=true`; start dev server. Iron Society demo gym should be Pro tier. For each item below, confirm BOTH: `[agent-echo] received:` in the console AND a smartgym_agent_logs row (status='sent', action_taken='echo-received').

Event-driven (7):
1. level-up — complete a session for a member near a level boundary (or POST /api/sessions/{id}/complete for a seeded session) → engagement-agent 'level-up'
2. streak-broken — complete a session for a member whose current_streak >= 2 with a >3-day gap since last session → engagement-agent 'streak-broken'
3. leaderboard-updated — any completed session → engagement-agent 'leaderboard-updated'
4. member-at-risk — open the owner dashboard at-risk view (GET /api/owner/at-risk) → retention-agent per at-risk member
5. challenge-ended — owner-complete an active challenge → growth-agent 'challenge-ended'
6. upgrade-opportunity — on a starter/growth test gym, create machines past the tier limit (403) → revenue-agent 'upgrade-opportunity' (fires despite tier: PLATFORM_EVENT)
7. new-gym-onboarded — register a fresh test gym via /signup → growth-agent 'new-gym-onboarded' (PLATFORM_EVENT)

Billing 3 (already wired pre-phase; verify forwarding now reaches the echo): fire via direct trigger-route curls with the internal key for events 'subscription-cancelled' (retention), 'payment-failed' (revenue), 'trial-ending-soon' (engagement), OR via Stripe CLI test events if convenient.

Cron 3+:
8. member-inactive-14d + machine-underutilized + checkin-sla-overdue — `curl -X POST http://localhost:3000/api/cron/agent-daily -H "x-smartgym-internal-key: <key>"` (seed data guarantees dormant members + unscanned machines)
9. weekly-summary + at-risk early warning — same curl against /api/cron/agent-weekly

Safety checks:
10. Cooldown: re-run the agent-daily curl immediately → new rows are status='skipped' with 'Cooldown window active'; no duplicate echo lines
11. Tier matrix: flip Iron Society to 'starter' (admin subscription route or SQL), re-run agent-daily → all its gym-scoped fires log 'skipped' (tier reason); flip to 'growth' → retention/engagement events fire, operations/growth/revenue events skip; restore 'pro'
12. Approve or veto the PLATFORM_EVENTS bypass design (items 6-7 firing regardless of tier)
  </how-to-verify>
  <verify>Manual: 13/13 automations echoed + logged; duplicate fire skip-logged; tier matrix exact; PLATFORM_EVENTS design approved.</verify>
  <done>User confirms all 13 automations verified end-to-end in staging with cooldown + tier gating holding.</done>
  <resume-signal>Type "approved" when all 13 automations + cooldown + tier matrix verified (and the PLATFORM_EVENTS design is accepted), or describe failures</resume-signal>
</task>

</tasks>

<verification>
- Automated gate green (Task 1) before the walkthrough starts
- Walkthrough evidence: 13/13 automations echoed + logged; duplicate-fire skipped; tier matrix exact
</verification>

<success_criteria>
- Phase 5 success criteria 1-5 (ROADMAP) all TRUE in staging; AGENT-05 satisfied; phase ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/05-uptimizeai-agent-connection/05-07-SUMMARY.md`
</output>
