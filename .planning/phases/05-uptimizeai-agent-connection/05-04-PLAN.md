---
phase: 05-uptimizeai-agent-connection
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - apps/web-admin/src/lib/billing/featureGate.ts
  - apps/web-admin/src/lib/billing/__tests__/featureGate-upgrade.test.ts
  - apps/web-admin/src/app/api/gym/[gymId]/machines/route.ts
  - apps/web-admin/src/app/api/onboard/register/route.ts
  - apps/web-admin/src/app/api/onboard/__tests__/register.test.ts
autonomous: true
requirements: [AGENT-03]
must_haves:
  truths:
    - "A gym hitting its machine tier limit fires revenue-agent 'upgrade-opportunity' (7-day cooldown per gym+feature via dedup_key)"
    - "Only upgrade-intent features can fire the nudge (guard list) — internal/low-value gates never spam the revenue agent"
    - "A successful new-gym registration fires growth-agent 'new-gym-onboarded' exactly once per gym"
    - "Registration and machine-creation user flows are never slowed or failed by agent calls"
  artifacts:
    - path: "apps/web-admin/src/lib/billing/featureGate.ts"
      provides: "shouldTriggerUpgradeAgent guard + fireUpgradeOpportunity helper"
      exports: ["shouldTriggerUpgradeAgent", "fireUpgradeOpportunity", "checkFeatureAccess", "checkAgentAccess"]
    - path: "apps/web-admin/src/app/api/gym/[gymId]/machines/route.ts"
      provides: "upgrade-opportunity call site at the machine-limit 403 denial"
      contains: "fireUpgradeOpportunity"
    - path: "apps/web-admin/src/app/api/onboard/register/route.ts"
      provides: "new-gym-onboarded call site after complete_gym_onboarding RPC success"
      contains: "new-gym-onboarded"
  key_links:
    - from: "apps/web-admin/src/app/api/gym/[gymId]/machines/route.ts"
      to: "apps/web-admin/src/lib/billing/featureGate.ts"
      via: "fireUpgradeOpportunity(gymId, 'max_machines') at the limit-reached 403"
      pattern: "fireUpgradeOpportunity"
    - from: "apps/web-admin/src/lib/billing/featureGate.ts"
      to: "@/lib/billing/triggerAgent"
      via: "triggerUptimizeAIAgent('revenue-agent', { event: 'upgrade-opportunity', dedup_key: feature })"
      pattern: "upgrade-opportunity"
    - from: "apps/web-admin/src/app/api/onboard/register/route.ts"
      to: "@/lib/billing/triggerAgent"
      via: "fire-and-forget after RPC success, before response"
      pattern: "triggerUptimizeAIAgent\\('growth-agent'"
---

<objective>
Wire the two platform-growth automations: #8 upgrade-opportunity (feature-gate denial → revenue-agent) and #13 new-gym-onboarded (registration success → growth-agent).

Purpose: completes the event-driven revenue/growth call sites of AGENT-03. Both events are in the PLATFORM_EVENTS allowlist established in 05-02 (they bypass gym-tier gating — see 05-02 objective for the rationale; without the bypass both automations would be permanently dead code since the firing gym never has Pro).

Ground truth constraint discovered in planning: `checkFeatureAccess` currently has ZERO callers in the codebase — the only live feature-gate denial surface is the machine tier-limit 403 in `gym/[gymId]/machines/route.ts` (TIER_LIMITS check). So this plan (a) builds the reusable guard+helper in featureGate.ts for all future gate callers, and (b) wires the one concrete denial surface that exists today.

Output: featureGate helpers (guarded, tested), machine-limit wiring, register wiring.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-uptimizeai-agent-connection/05-RESEARCH.md
@.planning/phases/05-uptimizeai-agent-connection/05-02-SUMMARY.md
@apps/web-admin/src/lib/billing/featureGate.ts
@apps/web-admin/src/app/api/gym/[gymId]/machines/route.ts
@apps/web-admin/src/app/api/onboard/register/route.ts
@apps/web-admin/src/app/api/onboard/__tests__/register.test.ts
</context>

<interfaces>
```typescript
// featureGate.ts existing exports (keep intact)
type FeatureKey = keyof TierFeatures; // max_machines, max_members, ai_programs, coach_notes,
  // custom_branding, franchise_support, leaderboards, challenges, social_feed,
  // push_notifications, occupancy_heatmap, uptimizeai_agents
export async function checkFeatureAccess(gymId: string, feature: FeatureKey): Promise<FeatureAccessResult>
export async function checkAgentAccess(gymId: string, agentName: string): Promise<FeatureAccessResult>
```

gym/[gymId]/machines route: after verifyStaff + gym_id ownership check, reads `gyms.subscription_tier`, computes `limit = TIER_LIMITS[tier] ?? 5`, and when `count >= limit` returns 403 `{ error: 'Machine limit reached for your tier (N)' }` — THIS is the wiring point.

onboard/register route: after `admin.rpc('complete_gym_onboarding', {...})` succeeds, `gymId` holds the new gym's uuid and the route returns `{ gym_id: gymId, email }` — fire between RPC success and the return. New gyms are created with `p_tier: 'starter'` (why the PLATFORM_EVENTS bypass exists).

Existing register.test.ts mocks `@/lib/supabase/server`, `@supabase/supabase-js`, `@/lib/rateLimit`, `@nexera/utils` — extend it, do not rewrite.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: featureGate upgrade helpers + machine-limit call site (RED then GREEN)</name>
  <files>apps/web-admin/src/lib/billing/featureGate.ts, apps/web-admin/src/lib/billing/__tests__/featureGate-upgrade.test.ts, apps/web-admin/src/app/api/gym/[gymId]/machines/route.ts</files>
  <behavior>
RED first — new `__tests__/featureGate-upgrade.test.ts` (mock `@/lib/billing/triggerAgent`):
- shouldTriggerUpgradeAgent: true for 'ai_programs', 'challenges', 'coach_notes', 'custom_branding', 'max_machines'; false for 'leaderboards', 'social_feed', 'push_notifications', 'franchise_support'
- fireUpgradeOpportunity('gym-1', 'max_machines') → triggerUptimizeAIAgent called with ('revenue-agent', { event: 'upgrade-opportunity', gym_id: 'gym-1', feature: 'max_machines', dedup_key: 'max_machines', is_agent_initiated: false, ... })
- fireUpgradeOpportunity('gym-1', 'leaderboards') → triggerUptimizeAIAgent NOT called (guard)
- fireUpgradeOpportunity never throws when the trigger rejects (internal .catch)
  </behavior>
  <action>
1. In featureGate.ts add (research Open Question 3 resolution):
```typescript
const UPGRADE_NUDGE_FEATURES: FeatureKey[] = ['ai_programs', 'challenges', 'coach_notes', 'custom_branding', 'max_machines'];
export function shouldTriggerUpgradeAgent(feature: FeatureKey): boolean
/** Fire-and-forget revenue-agent nudge. Guarded by UPGRADE_NUDGE_FEATURES; never throws.
 *  7-day per-(gym, feature) cooldown enforced by the trigger route via dedup_key. */
export function fireUpgradeOpportunity(gymId: string, feature: FeatureKey, requiredTier?: SubscriptionTier): void
```
fireUpgradeOpportunity imports `triggerUptimizeAIAgent` from './triggerAgent' (no import cycle: triggerAgent.ts imports nothing from featureGate). Payload includes `required_tier: requiredTier ?? null`. Wire it inside a `.catch(err => console.error('[featureGate] upgrade-opportunity trigger failed:', ...))`. Deliberately NOT called inside checkFeatureAccess itself (avoids firing from internal/admin bypass reads — research Q3).

2. In `gym/[gymId]/machines/route.ts`, in the `count >= limit` branch, before returning the 403: `fireUpgradeOpportunity(gymId, 'max_machines', 'growth');` (synchronous void call — response path unaffected). Import from '@/lib/billing/featureGate'.

3. RED → GREEN → commits (test(05-04): / feat(05-04): with Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>).
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="featureGate-upgrade" --silent && npx tsc --noEmit</automated>
  </verify>
  <done>Guard list enforced; helper fires the exact payload contract; machine-limit 403 nudges the revenue agent; existing suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: new-gym-onboarded on registration success (RED then GREEN)</name>
  <files>apps/web-admin/src/app/api/onboard/register/route.ts, apps/web-admin/src/app/api/onboard/__tests__/register.test.ts</files>
  <behavior>
Extend the EXISTING register.test.ts (add `@/lib/billing/triggerAgent` mock):
- Successful registration (signUp ok + RPC ok) → triggerUptimizeAIAgent called with ('growth-agent', { event: 'new-gym-onboarded', gym_id: <RPC result>, gym_name, gym_type, is_agent_initiated: false }) — no owner PII (no email) in the payload
- signUp duplicate (409 path) → no agent call
- RPC failure (rollback path) → no agent call
- Agent rejection does not change the 200 response
- All pre-existing register tests still pass unchanged
  </behavior>
  <action>
In onboard/register/route.ts, after the RPC error check and before `return NextResponse.json({ gym_id: gymId, email })`, add the fire-and-forget growth-agent call per the behavior spec, `.catch(err => console.error('[onboard/register] new-gym-onboarded trigger failed:', ...))`. Payload: gym_id, gym_name, gym_type (from parsed.data), is_agent_initiated: false. No dedup_key needed — 30-day cooldown per (gym, event) from the 05-02 map makes this once-per-gym in practice. Note in a comment: this is a PLATFORM_EVENT — it fires even though new gyms are 'starter' tier (see trigger route cooldown.ts rationale). RED → GREEN → commits.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="onboard" --silent && npx jest --silent && npx tsc --noEmit</automated>
  </verify>
  <done>Registration fires new-gym-onboarded once, only on full success, without PII; full web-admin suite green, tsc clean.</done>
</task>

</tasks>

<verification>
- featureGate-upgrade + register tests green; full suite green; tsc clean
- No `await triggerUptimizeAIAgent` / `await fireUpgradeOpportunity` in either route
</verification>

<success_criteria>
- Automation #8 (upgrade-opportunity) wired at the only live feature-gate denial surface, with a reusable guard+helper for future gate callers
- Automation #13 (new-gym-onboarded) wired at registration success
</success_criteria>

<output>
After completion, create `.planning/phases/05-uptimizeai-agent-connection/05-04-SUMMARY.md`
</output>
