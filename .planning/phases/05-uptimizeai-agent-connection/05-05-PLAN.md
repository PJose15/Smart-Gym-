---
phase: 05-uptimizeai-agent-connection
plan: 05
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - apps/web-admin/src/app/api/cron/agent-daily/route.ts
  - apps/web-admin/src/app/api/cron/agent-daily/__tests__/agent-daily.test.ts
autonomous: true
requirements: [AGENT-04]
must_haves:
  truths:
    - "The daily cron fires retention-agent 'member-inactive-14d' for every active member with no session in 14 days"
    - "The daily cron fires operations-agent 'checkin-sla-overdue' for check-ins pending trainer review past 48h"
    - "The daily cron fires operations-agent 'machine-underutilized' for machines with zero scans in 7 days"
    - "Expired challenges (end_date passed, still active) are auto-deactivated AND fire growth-agent 'challenge-ended' — deduped against the owner-complete path"
    - "The cron authenticates via x-smartgym-internal-key OR Authorization Bearer (pg_cron pattern) and processes in batches of 10 (no serverless timeout)"
  artifacts:
    - path: "apps/web-admin/src/app/api/cron/agent-daily/route.ts"
      provides: "4 daily agent scans (dormant, checkin SLA, machine underutilization, challenge auto-expiry)"
      exports: ["POST"]
      min_lines: 120
    - path: "apps/web-admin/src/app/api/cron/agent-daily/__tests__/agent-daily.test.ts"
      provides: "TDD coverage per scan type with mocked DB returns"
      min_lines: 120
  key_links:
    - from: "apps/web-admin/src/app/api/cron/agent-daily/route.ts"
      to: "@/lib/billing/triggerAgent"
      via: "batched Promise.allSettled fire-and-forget triggers"
      pattern: "triggerUptimizeAIAgent"
    - from: "supabase migration 029 cron.schedule 'nexera-agent-daily'"
      to: "apps/web-admin/src/app/api/cron/agent-daily/route.ts"
      via: "net.http_post daily 06:00 UTC with Bearer key"
      pattern: "authorization"
---

<objective>
Create `/api/cron/agent-daily` — the daily scan route covering scheduled automations #3 (member-inactive-14d → retention), #10 (machine-underutilized → operations), #11 (checkin-sla-overdue → operations), plus the challenge auto-expiry safety net for #12 (challenge-ended → growth; Pitfall 6 — challenges that end by date without the owner ever clicking Complete).

Purpose: AGENT-04's daily half. The pg_cron schedule already exists (migration 029, plan 05-01) pointing at this route. Per-target cooldowns are enforced downstream by the trigger route (05-02) — re-running the cron never double-fires within a window.

Output: cron route + test file, following the checkin-deadline/dna-recompute conventions exactly.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-uptimizeai-agent-connection/05-RESEARCH.md
@.planning/phases/05-uptimizeai-agent-connection/05-02-SUMMARY.md
@apps/web-admin/src/app/api/cron/checkin-deadline/route.ts
@apps/web-admin/src/app/api/cron/dna-recompute/route.ts
@apps/web-admin/src/lib/billing/triggerAgent.ts
</context>

<interfaces>
Cron auth pattern (copy from dna-recompute — accepts BOTH headers because pg_cron sends Bearer):
```typescript
const providedKey =
  request.headers.get('x-smartgym-internal-key') ??
  request.headers.get('authorization')?.replace('Bearer ', '') ?? null;
if (providedKey !== internalKey) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
```

Batch pattern (copy from dna-recompute): slice into batches of 10, `await Promise.allSettled(batch.map(...))` per batch.

Schema ground truth (verified against migration 001):
- `members`: id, gym_id, status, last_session_date (date)
- `weekly_checkins`: id, member_id, gym_id, trainer_id, trainer_approved (bool), sent_at, created_at — the overdue filter used by checkin-deadline: `.eq('trainer_approved', false).is('sent_at', null).not('trainer_id', 'is', null).lt('created_at', fortyEightHoursAgo)`
- `gym_challenges`: id, gym_id, is_active (bool), end_date (DATE, not timestamptz — compare against a YYYY-MM-DD string)
- `machines`: id, gym_id (verify exact active-flag column name in migration 001 before filtering; if none, scan all machines)
- `machine_scan_events`: verify exact column names (machine_id + timestamp column) in migration 001 before writing the query
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route skeleton + dormant-member and checkin-SLA scans (RED then GREEN)</name>
  <files>apps/web-admin/src/app/api/cron/agent-daily/route.ts, apps/web-admin/src/app/api/cron/agent-daily/__tests__/agent-daily.test.ts</files>
  <behavior>
RED first — new `__tests__/agent-daily.test.ts` (@jest-environment node). Mock `@supabase/supabase-js` createClient (chainable builder returning canned per-table data) + `@/lib/billing/triggerAgent`. Set process.env.INTERNAL_WEBHOOK_KEY in beforeEach. Tests:
- No key header → 401; wrong key → 401; valid x-smartgym-internal-key → 200; valid Authorization Bearer → 200
- 2 dormant members returned → triggerUptimizeAIAgent called twice with ('retention-agent', { event: 'member-inactive-14d', gym_id, member_id, is_agent_initiated: false })
- 1 overdue check-in returned → ('operations-agent', { event: 'checkin-sla-overdue', gym_id, member_id, dedup_key: <checkin id>, is_agent_initiated: false })
- Empty scans → 200 with all counters 0, no trigger calls
- One trigger rejecting does not fail the route (Promise.allSettled)
  </behavior>
  <action>
Create `apps/web-admin/src/app/api/cron/agent-daily/route.ts`:
- `export const dynamic = 'force-dynamic'`; POST only; dual-header auth (see interfaces); inline getAdminClient() like checkin-deadline.
- Scan 1 — dormant: `members.select('id, gym_id').eq('status','active').lt('last_session_date', fourteenDaysAgoDateString)` → per member fire retention-agent 'member-inactive-14d'. Batches of 10 via Promise.allSettled.
- Scan 2 — checkin SLA: same overdue filter as checkin-deadline (select id, member_id, gym_id) → per check-in fire operations-agent 'checkin-sla-overdue' with dedup_key = checkin.id.
- Return JSON counters: `{ dormant_triggered, checkins_triggered, ... }` (extended in Task 2).
- Route file exports only POST + dynamic; any pure helpers go in an adjacent module if needed.
RED → GREEN → commits (test(05-05): / feat(05-05): with Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>).
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="agent-daily" --silent && npx tsc --noEmit</automated>
  </verify>
  <done>Auth (both header forms) + dormant + checkin-SLA scans fire the correct agents/events with batching; counters returned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Machine-underutilization + challenge auto-expiry scans (RED then GREEN)</name>
  <files>apps/web-admin/src/app/api/cron/agent-daily/route.ts, apps/web-admin/src/app/api/cron/agent-daily/__tests__/agent-daily.test.ts</files>
  <behavior>
Extend agent-daily.test.ts:
- 3 machines, scan events exist for 1 of them in the last 7d → operations-agent 'machine-underutilized' fired for exactly the 2 unscanned machines, each with dedup_key = machine id and gym_id
- 1 active challenge with end_date before today → gym_challenges update called setting is_active=false AND growth-agent fired with ('growth-agent', { event: 'challenge-ended', gym_id, dedup_key: <challenge id>, challenge_id, auto_expired: true, is_agent_initiated: false })
- Active challenge ending in the future → not deactivated, no fire
- Response includes machines_triggered and challenges_expired counters
  </behavior>
  <action>
Extend the route:
- Scan 3 — machine underutilization: FIRST verify exact column names in `supabase/migrations/001_nexera_schema.sql` for `machines` (active flag, if any) and `machine_scan_events` (machine_id + its timestamp column). Then: fetch all machines (id, gym_id), fetch distinct machine_ids from machine_scan_events in the trailing 7 days, diff in memory (Set), fire operations-agent 'machine-underutilized' per zero-scan machine with `machine_id` + `dedup_key: machine.id`. Batches of 10.
- Scan 4 — challenge auto-expiry: `gym_challenges.select('id, gym_id').eq('is_active', true).lt('end_date', todayDateString)` where todayDateString = `new Date().toISOString().slice(0,10)` (end_date is a DATE column). For each: update is_active=false, then fire growth-agent 'challenge-ended' with `dedup_key: challenge.id, auto_expired: true`. The dedup_key matches the owner-complete path from plan 05-03 — the trigger route's 24h challenge-ended cooldown guarantees once-per-challenge across both paths (Pitfall 6).
- Extend the response counters. RED → GREEN → commits.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="agent-daily" --silent && npx jest --silent && npx tsc --noEmit</automated>
  </verify>
  <done>All 4 daily scans live with correct agents, events, dedup keys, and batching; full web-admin suite green, tsc clean.</done>
</task>

</tasks>

<verification>
- agent-daily tests green covering all 4 scan types + auth + batching resilience
- Full web-admin suite green, tsc clean
- Manual smoke (optional, needs dev server): `curl -X POST http://localhost:3000/api/cron/agent-daily -H "x-smartgym-internal-key: $INTERNAL_WEBHOOK_KEY"` returns counters JSON
</verification>

<success_criteria>
- Scheduled automations #3, #10, #11 fire from the daily cron; challenge auto-expiry closes the #12 gap for date-expired challenges
</success_criteria>

<output>
After completion, create `.planning/phases/05-uptimizeai-agent-connection/05-05-SUMMARY.md`
</output>
