---
phase: 05-uptimizeai-agent-connection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/029_agent_dedup_and_cron.sql
  - apps/web-admin/src/app/api/dev/agent-echo/route.ts
  - apps/web-admin/.env.example
  - DEMO_SETUP.md
autonomous: false
requirements: [AGENT-02, AGENT-04, AGENT-05]
must_haves:
  truths:
    - "Dedup queries on smartgym_agent_logs are index-backed per (gym, agent, event, time window)"
    - "agent-daily and agent-weekly cron routes are scheduled in pg_cron (daily 06:00 UTC / Sunday 05:00 UTC)"
    - "A staging operator can point UPTIMIZE_WEBHOOK_URL at a local echo receiver and see every forwarded agent call confirmed in the DB"
    - "Going live with the real UptimizeAI engine requires changing only UPTIMIZE_WEBHOOK_URL (+key) — no code changes"
  artifacts:
    - path: "supabase/migrations/029_agent_dedup_and_cron.sql"
      provides: "Partial dedup indexes + 2 pg_cron schedules"
      contains: "idx_agent_logs_dedup"
    - path: "apps/web-admin/src/app/api/dev/agent-echo/route.ts"
      provides: "DEMO_ECHO_AGENTS-gated echo receiver for staging verification"
      exports: ["POST"]
    - path: "apps/web-admin/.env.example"
      provides: "UPTIMIZE_WEBHOOK_URL / UPTIMIZE_API_KEY / DEMO_ECHO_AGENTS env contract"
      contains: "UPTIMIZE_WEBHOOK_URL"
  key_links:
    - from: "supabase/migrations/029_agent_dedup_and_cron.sql"
      to: "/api/cron/agent-daily and /api/cron/agent-weekly"
      via: "cron.schedule + net.http_post (mirrors migration 010 pattern)"
      pattern: "net\\.http_post"
    - from: "apps/web-admin/src/app/api/dev/agent-echo/route.ts"
      to: "smartgym_agent_logs"
      via: "update action_taken='echo-received' by log_id"
      pattern: "echo-received"
---

<objective>
Lay the Phase 5 foundation: migration 029 (agent-log dedup indexes + pg_cron schedules for the two new agent cron routes), the staging echo receiver, and the UptimizeAI env contract.

Purpose: AGENT-02's cooldown dedup (built in plan 05-02) needs index-backed log queries; AGENT-04's cron routes (plans 05-05/05-06) need pg_cron schedules; AGENT-05's staging verification needs a verifiable receiving endpoint since UptimizeAI is NOT yet a real external service (LOCKED user decision: staging-ready build — all forwarding goes to configurable UPTIMIZE_WEBHOOK_URL; going live later = set one env var; NO real-endpoint credential wiring).

Dispatcher resolution (per 05-RESEARCH.md Pattern 7, Option B — adopted): Phase 5 builds NO notification dispatcher. Agent outputs are scoped to smartgym_agent_logs rows + echo-receiver confirmation. Member push delivery activates in Phase 6, which consumes the `is_agent_initiated` loop-safety flag that plan 05-02 puts in the payload schema.

Output: migration file (applied to live DB via human checkpoint), echo route, env contract docs.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-uptimizeai-agent-connection/05-RESEARCH.md
@supabase/migrations/010_performance_dna.sql
@apps/web-admin/.env.example
</context>

<interfaces>
Existing smartgym_agent_logs columns (migration 001): id, gym_id, member_id, agent_name, trigger_event, action_taken, channel, status CHECK IN ('sent','failed','pending','skipped'), error_message, payload jsonb, executed_at timestamptz DEFAULT now().
IMPORTANT: the status CHECK constraint does NOT allow 'echoed' — the echo receiver must record reception via the `action_taken` text column, never a new status value.

pg_cron HTTP-call pattern (from migration 010, lines 145-158 — copy exactly):
```sql
SELECT cron.schedule(
  'nexera-compute-weekly-dna',
  '30 4 * * 0',
  $$
  SELECT net.http_post(
    url := current_setting('app.settings.base_url', true) || '/api/cron/dna-recompute',
    headers := jsonb_build_object(
      'Authorization', 'Bearer ' || current_setting('app.settings.service_role_key', true),
      'Content-Type', 'application/json'
    ),
    body := '{}'::jsonb
  )
  $$
);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 029 — dedup indexes + agent cron schedules</name>
  <files>supabase/migrations/029_agent_dedup_and_cron.sql</files>
  <action>
Create migration 029 (next free number — 001-028 exist) with:

1. Two partial indexes for cooldown dedup queries (from 05-RESEARCH.md, verified against migration 001 schema):
```sql
CREATE INDEX IF NOT EXISTS idx_agent_logs_dedup
  ON smartgym_agent_logs(gym_id, agent_name, trigger_event, executed_at DESC)
  WHERE status = 'sent';

CREATE INDEX IF NOT EXISTS idx_agent_logs_member_dedup
  ON smartgym_agent_logs(gym_id, agent_name, member_id, executed_at DESC)
  WHERE member_id IS NOT NULL AND status = 'sent';
```

2. Two pg_cron schedules using the exact net.http_post pattern from migration 010 (see interfaces block):
   - `nexera-agent-daily`, cron `0 6 * * *`, POST to `/api/cron/agent-daily`
   - `nexera-agent-weekly`, cron `0 5 * * 0`, POST to `/api/cron/agent-weekly`
   Both with the `Authorization: Bearer || current_setting('app.settings.service_role_key', true)` header pattern (the cron routes accept Bearer fallback, same as dna-recompute).

Add a header comment noting: (a) the routes are created in plans 05-05/05-06 of this phase — until the code deploys, the scheduled net.http_post calls 404 harmlessly; (b) is_agent_initiated lives in the payload jsonb, so no column migration is needed for it.

Use `gen_random_uuid()` conventions if any DDL needs it (repo convention — none expected here).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/029_agent_dedup_and_cron.sql','utf8');if(!/idx_agent_logs_dedup/.test(s)||!/idx_agent_logs_member_dedup/.test(s)||!/nexera-agent-daily/.test(s)||!/nexera-agent-weekly/.test(s)||!/net\.http_post/.test(s))process.exit(1);console.log('migration 029 OK')"</automated>
  </verify>
  <done>Migration file exists with both partial indexes and both cron schedules following the migration 010 net.http_post pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Echo receiver route + env contract</name>
  <files>apps/web-admin/src/app/api/dev/agent-echo/route.ts, apps/web-admin/.env.example, DEMO_SETUP.md</files>
  <action>
1. Create `apps/web-admin/src/app/api/dev/agent-echo/route.ts` (POST only, `export const dynamic = 'force-dynamic'`):
   - If `process.env.DEMO_ECHO_AGENTS !== 'true'` → return 404 `{ error: 'Not available' }` (hard gate; never active in production).
   - Parse JSON body (return 400 on invalid JSON). Expected shape from the trigger route forward (plan 05-02): `{ agent_name, payload, log_id }`.
   - `console.log('[agent-echo] received:', JSON.stringify(body))`.
   - If `body.log_id` is a string: with a service-role admin client (same inline `getAdminClient()` pattern as `api/cron/checkin-deadline/route.ts`), `update smartgym_agent_logs set action_taken = 'echo-received' where id = log_id`. Do NOT touch the `status` column (CHECK constraint only allows sent/failed/pending/skipped).
   - Return 200 `{ echoed: true, agent_name: body.agent_name ?? null, log_id: body.log_id ?? null }`.
   - Route file exports ONLY the HTTP handler + dynamic const (repo convention from Phase 1: route files export HTTP methods only).

2. Append to `apps/web-admin/.env.example` in the TIER 2 server-only section, after `INTERNAL_WEBHOOK_KEY=`:
```
# — UptimizeAI agent engine —
# Staging: point at the dev echo receiver and set DEMO_ECHO_AGENTS=true:
#   UPTIMIZE_WEBHOOK_URL=http://localhost:3000/api/dev/agent-echo
# Going live: set UPTIMIZE_WEBHOOK_URL to the real UptimizeAI endpoint (contact UptimizeAI team
# for the URL + API key). No code changes required. Leave unset to disable forwarding entirely.
UPTIMIZE_WEBHOOK_URL=
UPTIMIZE_API_KEY=
DEMO_ECHO_AGENTS=false
```

3. Add a short "Agent staging verification" section to `DEMO_SETUP.md`: set the two env values above + DEMO_ECHO_AGENTS=true, restart dev server, fire any agent trigger, confirm `[agent-echo] received:` in console and `action_taken='echo-received'` on the matching smartgym_agent_logs row.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx tsc --noEmit</automated>
  </verify>
  <done>Echo route compiles, is DEMO_ECHO_AGENTS-gated (404 otherwise), records echo reception via action_taken; .env.example documents the three new vars with the staging-vs-live story; DEMO_SETUP.md explains the verification loop.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Approve + apply migration 029 to live DB</name>
  <files>supabase/migrations/029_agent_dedup_and_cron.sql</files>
  <action>
Human-only step (repo convention: ANY db push to live requires user approval, same as 01-01/03-01). Claude has authored migration 029 in Task 1; the user reviews and applies it to the live Supabase project. Claude must NOT apply the migration itself — pause and wait for the resume signal.
  </action>
  <what-built>Migration 029: two partial dedup indexes on smartgym_agent_logs + pg_cron schedules for the upcoming agent-daily/agent-weekly cron routes. Index-only + scheduling — no table/column/RLS changes.</what-built>
  <how-to-verify>
1. Review `supabase/migrations/029_agent_dedup_and_cron.sql`.
2. Apply to the live Supabase project (aztppxuapbgmadfigtys) — same convention as migrations 027/028 (supabase MCP apply_migration, or `supabase db push`).
3. Confirm: `select indexname from pg_indexes where tablename='smartgym_agent_logs';` shows both new indexes, and `select jobname from cron.job where jobname like 'nexera-agent-%';` shows both jobs.
Note: the scheduled jobs will 404 harmlessly until plans 05-05/05-06 deploy the routes.
  </how-to-verify>
  <verify>Manual: pg_indexes shows idx_agent_logs_dedup + idx_agent_logs_member_dedup; cron.job shows nexera-agent-daily + nexera-agent-weekly.</verify>
  <done>User confirms migration 029 applied to live DB with both indexes and both cron jobs present.</done>
  <resume-signal>Type "applied" once migration 029 is live, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Migration 029 file matches repo migration conventions and is applied to live DB (human-confirmed)
- Echo route: 404 when DEMO_ECHO_AGENTS unset; echoes + marks log row when set
- tsc clean; existing web-admin suite (347) untouched and green
</verification>

<success_criteria>
- Dedup indexes live on smartgym_agent_logs
- nexera-agent-daily + nexera-agent-weekly registered in cron.job
- Env contract documented; echo receiver ready as the staging verification target (LOCKED decision honored: no real-endpoint credential wiring)
</success_criteria>

<output>
After completion, create `.planning/phases/05-uptimizeai-agent-connection/05-01-SUMMARY.md`
</output>
