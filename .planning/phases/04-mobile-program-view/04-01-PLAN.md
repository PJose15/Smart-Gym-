---
phase: 04-mobile-program-view
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/lib/programLogic.ts
  - apps/mobile/src/lib/programService.ts
  - apps/mobile/src/lib/cacheManager.ts
  - apps/mobile/src/lib/__tests__/programLogic.test.ts
  - apps/mobile/src/lib/__tests__/programService.test.ts
autonomous: true
requirements: [PROG-01, PROG-02, PROG-04]

must_haves:
  truths:
    - "fetchProgram returns a fully-shaped ActiveProgram (title, goal, weeks, sessions/week, trainer_name, days) for a member with an active ai_programs row"
    - "fetchProgram returns null (not a throw) when the member has no active program"
    - "trainer_name resolves from users.display_name when trainer_approved_by is set, and stays null when it is not"
    - "Pure logic answers: week progress pct, which day is today, which days are complete — all deterministic via injected now"
  artifacts:
    - path: "apps/mobile/src/lib/programLogic.ts"
      provides: "Pure program display logic (no Supabase, no React) mirroring challengeLogic.ts"
      exports: ["programProgressPct", "resolveTodayDayNumber", "dayStatus", "weekLabel", "formatSetsReps", "buildMetaChips"]
    - path: "apps/mobile/src/lib/programService.ts"
      provides: "Data layer: fetchProgram + PROGRAM_CACHE_KEY + ActiveProgram/ProgramDay/ProgramExercise types"
      exports: ["fetchProgram", "PROGRAM_CACHE_KEY", "ActiveProgram", "ProgramDay", "ProgramExercise"]
    - path: "apps/mobile/src/lib/__tests__/programLogic.test.ts"
      provides: "Unit tests for all pure functions incl. parity with @nexera/utils getTodaysProgramDay"
      min_lines: 80
    - path: "apps/mobile/src/lib/__tests__/programService.test.ts"
      provides: "Service tests: happy path, null path, trainer_name resolution, malformed program_data, cache key"
      min_lines: 80
  key_links:
    - from: "apps/mobile/src/lib/programService.ts"
      to: "ai_programs table"
      via: "supabase.from('ai_programs') direct read under RLS"
      pattern: "from\\('ai_programs'\\)"
    - from: "apps/mobile/src/lib/programService.ts"
      to: "users table"
      via: "trainer name lookup guarded by trainer_approved_by null check"
      pattern: "trainer_approved_by"
    - from: "apps/mobile/src/lib/cacheManager.ts"
      to: "programService consumers"
      via: "CacheTTL.programData constant"
      pattern: "programData"
---

<objective>
Build the Phase 4 data layer with TDD: a pure logic module (`programLogic.ts`, mirroring `challengeLogic.ts`) and a Supabase service (`programService.ts`, mirroring `challengeService.ts`) that fetches a member's active program from `ai_programs` — handling BOTH AI-generated and trainer-approved programs (both live in `ai_programs`; trainer-approved rows have `trainer_approved_by` set, requiring a `users.display_name` lookup).

Purpose: Everything the program screen (plan 04-02) renders comes from these two modules. Getting the logic right here — especially "which day is today" (must match the home screen's TodayZone) and the null/empty-program path (PROG-04) — makes the UI plan pure assembly.
Output: 2 new lib modules + ~20 new unit tests, `CacheTTL.programData` entry, mobile suite green (189 baseline + new), tsc clean.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-mobile-program-view/04-RESEARCH.md
@apps/mobile/src/lib/challengeLogic.ts
@apps/mobile/src/lib/challengeService.ts
@apps/mobile/src/lib/__tests__/challengeService.test.ts
@apps/mobile/src/lib/cacheManager.ts

<interfaces>
<!-- Contracts the executor needs. No codebase exploration required beyond the @files above. -->

**Target column set** (from `apps/web-admin/src/app/api/member/[memberId]/program/route.ts` — the ground-truth field list):
```
id, title, description, goal, duration_weeks, sessions_per_week, week_number,
day_number, sessions_completed, sessions_total, on_track, program_data,
generated_by, trainer_approved, trainer_approved_by, created_at
```

**program_data jsonb shape** (confirmed from seed data; field is `exercise_name`, NOT `name`/`machine_name` — research Pitfall 1):
```typescript
interface ProgramExercise {
  exercise_name: string;
  machine_id: string | null;
  default_sets: number;
  default_reps: number;
}
interface ProgramDay { day_number: number; name: string; exercises: ProgramExercise[]; }
// ai_programs.program_data = { days: ProgramDay[] }
```

**Today formula** (from `packages/utils/src/index.ts` getTodaysProgramDay — programLogic must mirror this exactly, with injected `now`):
```typescript
// totalDays <= 0 → 1; invalid assignedAt date → 1
// otherwise: floor((now - assigned) / 86400000) % totalDays + 1
```

**Supabase chainable mock** (from challengeService.test.ts — reuse `makeSupabaseMock` verbatim; global supabase mock lives in jest.setup.js, re-import `supabase` from `../supabase` and set `mockSupabase.from = jest.fn()...` per test):
```typescript
function makeSupabaseMock(resolvedValue: unknown): any // chain methods return this; single/maybeSingle/then resolve
```

**CacheTTL / CacheKey** (from cacheManager.ts): `CacheTTL` is a `const` object; `CacheKey = keyof typeof CacheTTL | (string & Record<string, never>)`. Dynamic keys need `as CacheKey` cast at call sites (STATE.md decision, research Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for programLogic and programService</name>
  <files>apps/mobile/src/lib/__tests__/programLogic.test.ts, apps/mobile/src/lib/__tests__/programService.test.ts</files>
  <behavior>
    **programLogic.test.ts** (import from `../programLogic` — module does not exist yet; tests MUST fail):
    - `programProgressPct(completed, total)`: 0 when total <= 0; 0 when completed < 0; 50 for (8, 16); clamps to 100 when completed >= total; handles non-integer ratio (5/16 → 31.25)
    - `resolveTodayDayNumber(assignedAt, totalDays, now)`: returns 1 when totalDays <= 0; returns 1 for invalid assignedAt string; day 1 on assignment day; cycles — assigned 4 days ago with totalDays 4 → day 1 again; assigned 5 days ago with totalDays 4 → day 2; **parity test**: for `assignedAt` = 3 days before `new Date()` and totalDays 4, result equals `getTodaysProgramDay(assignedAt, 4)` from `@nexera/utils`
    - `dayStatus(dayNumber, todayDayNumber)`: 'complete' when dayNumber < today; 'today' when equal; 'upcoming' when greater
    - `weekLabel(weekNumber, durationWeeks)`: "Week 2 of 8"; clamps week below 1 → "Week 1 of 8"; clamps above duration → "Week 8 of 8"
    - `formatSetsReps(3, 10)` → "3×10" (multiplication sign, matches TodayZone rendering)
    - `buildMetaChips(program)`: with goal 'hypertrophy', 8 weeks, 4/week, trainer 'Sam' → ['Hypertrophy', '8 weeks', '4x/week', 'Coach Sam']; null goal omitted; null trainer_name + generated_by → 'AI Coach' chip; 1 week → '1 week' (singular)
    - `PROGRAM_CACHE_KEY('mem-1')` → 'program:mem-1' (import from `../programService`)

    **programService.test.ts** (mirror challengeService.test.ts mocking; `jest.clearAllMocks()` in beforeEach):
    - fetchProgram returns null when maybeSingle resolves `{ data: null }`
    - fetchProgram happy path (trainer_approved_by: null): returns ActiveProgram with all scalar fields mapped, `days` parsed from program_data, `trainer_name: null`, and **only one** `supabase.from` call (no users lookup)
    - fetchProgram with trainer_approved_by set: second from('users') mock returns `{ data: { display_name: 'Coach Sam' } }` → trainer_name 'Coach Sam'
    - fetchProgram with trainer_approved_by set but users lookup returns `{ data: null }` → trainer_name null (no throw)
    - fetchProgram with malformed program_data (`null` and `{}` — no days key) → `days: []`, no throw
    - fetchProgram queries with `.eq('member_id', ...)`, `.eq('is_active', true)`, `.order('created_at', { ascending: false })`, `.limit(1)` (assert on the mock)
  </behavior>
  <action>
    Write both test files following the exact conventions of `challengeService.test.ts` (copy `makeSupabaseMock` helper) and `challengeLogic.test.ts` (pure function tables, injected `now: Date`). For the multi-table service tests, make `mockSupabase.from` return different chain mocks per table name (e.g. `jest.fn((table) => table === 'users' ? usersMock : programMock)`).

    Run the tests and CONFIRM THEY FAIL (modules don't exist). Commit:
    `test(04-01): add failing tests for program data layer`
    with Co-Authored-By: Claude Fable 5 &lt;noreply@anthropic.com&gt;
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest --testPathPattern="programLogic|programService" --silent 2>&1 | grep -E "Cannot find module|failed"</automated>
  </verify>
  <done>Both test files exist, run, and fail solely because `../programLogic` and `../programService` do not exist. RED commit created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement programLogic, programService, CacheTTL.programData</name>
  <files>apps/mobile/src/lib/programLogic.ts, apps/mobile/src/lib/programService.ts, apps/mobile/src/lib/cacheManager.ts</files>
  <behavior>All Task 1 tests pass; existing 189 mobile tests stay green; tsc clean.</behavior>
  <action>
    **programLogic.ts** — pure module, header comment mirroring challengeLogic.ts ("no Supabase, no React, no side effects; time-dependent functions accept injected now"):
    - `programProgressPct(sessionsCompleted: number, sessionsTotal: number): number` — 0 when total <= 0 or completed < 0; else min(100, completed/total*100)
    - `resolveTodayDayNumber(assignedAt: string, totalDays: number, now: Date): number` — deterministic mirror of `getTodaysProgramDay` from @nexera/utils (same guards, same formula). Doc comment MUST state: "Mirrors @nexera/utils getTodaysProgramDay with injected now — the home screen TodayZone uses that helper with ai_programs.created_at, and the program screen must highlight the SAME day (research Open Question 2 resolved: home-screen consistency wins over DB day_number)."
    - `dayStatus(dayNumber: number, todayDayNumber: number): 'complete' | 'today' | 'upcoming'` — complete when < today (done this cycle, per research Pitfall 4: do NOT try to map sessions_completed to specific days)
    - `weekLabel(weekNumber: number, durationWeeks: number): string` — clamp weekNumber to [1, max(1, durationWeeks)]
    - `formatSetsReps(sets: number, reps: number): string` — `${sets}×${reps}`
    - `buildMetaChips(program: { goal: string | null; duration_weeks: number; sessions_per_week: number; trainer_name: string | null }): string[]` — capitalized goal (omit when null), "N week(s)", "Nx/week", then "Coach {trainer_name}" when set else "AI Coach"

    **programService.ts** — mirror challengeService.ts structure (header comment documenting RLS ground truth: `programs_own` policy on ai_programs + migration 026 grants; anti-pattern note: do NOT query program_days/program_exercises tables or join member_program_assignments — program_data jsonb is the source of truth):
    - Export `ProgramExercise`, `ProgramDay`, `ActiveProgram` interfaces exactly as specified in the plan's interfaces block, with ActiveProgram carrying: id, title, description, goal, duration_weeks, sessions_per_week, week_number, day_number, sessions_completed, sessions_total, on_track, days, generated_by, trainer_approved, trainer_name, created_at
    - `PROGRAM_CACHE_KEY(memberId: string): string` → `program:${memberId}`
    - `fetchProgram(memberId: string): Promise<ActiveProgram | null>` — select the full column set from the interfaces block, `.eq('member_id', memberId).eq('is_active', true).order('created_at', { ascending: false }).limit(1).maybeSingle()`; return null when no row; guard trainer lookup with `if (program.trainer_approved_by)` before querying `users.display_name` (`??` to null); parse `program_data` as `{ days?: ProgramDay[] } | null` with `?? []` fallback; `trainer_approved: program.trainer_approved ?? false`

    **cacheManager.ts** — add one line to CacheTTL: `programData: 5 * 60 * 1000,  // 5 minutes` (program changes infrequently mid-session).

    Run the targeted tests, then the FULL mobile suite and tsc. Commit:
    `feat(04-01): program data layer — programLogic + programService + cache TTL`
    with Co-Authored-By: Claude Fable 5 &lt;noreply@anthropic.com&gt;
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx jest --silent</automated>
  </verify>
  <done>Targeted programLogic/programService tests all pass; full mobile suite green (189 baseline + ~20 new); tsc clean; GREEN commit created.</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx jest --testPathPattern="programLogic|programService" --silent` — all new tests pass
- `cd apps/mobile && npx tsc --noEmit && npx jest --silent` — full suite green, no regressions
- `grep -n "programData" apps/mobile/src/lib/cacheManager.ts` — TTL entry exists
- No imports of React or react-native in programLogic.ts (pure module)
</verification>

<success_criteria>
- fetchProgram handles all four data shapes: no program (null), AI-only program (trainer_name null), trainer-approved program (trainer_name resolved), malformed program_data (days: [])
- resolveTodayDayNumber proven equivalent to @nexera/utils getTodaysProgramDay by a parity test
- Two atomic TDD commits (test → feat), mobile suite green, tsc clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-mobile-program-view/04-01-SUMMARY.md`
</output>
