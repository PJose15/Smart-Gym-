---
phase: 04-mobile-program-view
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - apps/mobile/src/components/home/TodayZone.tsx
autonomous: true
requirements: [PROG-01, PROG-03]

must_haves:
  truths:
    - "Member with a program sees a 'View full program' link in the home TodayZone card and tapping it opens the /program screen"
    - "The link appears in BOTH TodayZone program branches (workout pending and workout done) — never in the freestyle/no-program branch"
    - "Full mobile suite green, web-admin suite green, both tsc clean — phase-gate quality bar"
  artifacts:
    - path: "apps/mobile/src/components/home/TodayZone.tsx"
      provides: "'View full program →' entry link routing to /program"
      contains: "router.push('/program')"
  key_links:
    - from: "apps/mobile/src/components/home/TodayZone.tsx"
      to: "apps/mobile/app/program/index.tsx"
      via: "router.push('/program') on the new link"
      pattern: "push\\('/program'\\)"
---

<objective>
Wire the discovery entry point — a "View full program →" link in the home screen's TodayZone card — and run the full Phase 4 automated gate.

Purpose: PROG-01 says the member can OPEN the full program view; without an entry point the screen is unreachable. TodayZone is the natural surface (research Pattern 4 / Open Question 1: member sees today's slice, taps to see the whole week). The gate then proves the phase left the codebase green.
Output: TodayZone link + phase gate evidence (test counts, tsc, hex scan) recorded in the SUMMARY.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-mobile-program-view/04-RESEARCH.md
@apps/mobile/src/components/home/TodayZone.tsx

<interfaces>
TodayZone branch structure (current file):
1. `activeWorkoutId` → "Continue Workout" card (no link needed — member is mid-workout)
2. `todayWorkout && todayDone` → "All done for today!" card → ADD LINK (member reviewing their plan)
3. `todayWorkout` → today's exercises + Start CTA → ADD LINK (primary discovery surface)
4. no program → freestyle prompt → NO LINK (nothing to view; /program would show empty state redundantly)

Target route already registered (plan 04-02): `app/program/index.tsx`, title "Your Program".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: "View full program" link in TodayZone</name>
  <files>apps/mobile/src/components/home/TodayZone.tsx</files>
  <action>
    Add a shared link element rendered in branches 2 and 3 (see interfaces block):
    - Branch 3 (has program, not done): a `TouchableOpacity` BELOW the "Start today's workout" CTA — quiet secondary link, centered: text "View full program →" in colors.textSecondary, typography.fontMedium, size 13, paddingVertical spacing.sm. It must NOT compete visually with the primary CTA (no background, no border).
    - Branch 2 (todayDone): same link below the nextPreview/tip/coaching blocks.
    - `onPress={() => router.push('/program')}`, `accessibilityRole="link"`, `accessibilityLabel="View full program"`.
    - Extract as a small local component (e.g. `ViewProgramLink`) inside the file to avoid duplicating styles across the two branches.
    - Do NOT touch branch 1 (active workout) or branch 4 (freestyle). No other TodayZone changes — styles use existing token imports only.

    Commit: `feat(04-03): TodayZone entry link to full program view`
    with Co-Authored-By: Claude Fable 5 &lt;noreply@anthropic.com&gt;
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && grep -c "ViewProgramLink" src/components/home/TodayZone.tsx | awk '{exit ($1 >= 3) ? 0 : 1}'</automated>
  </verify>
  <done>Link component defined once and rendered in exactly the two program branches; tsc clean; router.push('/program') present.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 4 automated gate</name>
  <files>(none — verification only)</files>
  <action>
    Run the full phase gate and record every result for the SUMMARY:
    1. `cd apps/mobile && npx tsc --noEmit` — clean
    2. `cd apps/mobile && npx jest --silent` — ALL suites green; count must be ≥ 209 (189 baseline + ≥ 20 from plan 04-01); record exact number
    3. `cd apps/web-admin && npx tsc --noEmit && npx jest --silent` — 347 web-admin tests still green (Phase 4 touched nothing in web-admin; this proves it)
    4. Hex scan on all Phase 4 files: `grep -rn "#[0-9a-fA-F]\{3,6\}" apps/mobile/src/lib/programLogic.ts apps/mobile/src/lib/programService.ts apps/mobile/src/components/program/ apps/mobile/src/components/skeleton/ProgramScreenSkeleton.tsx apps/mobile/app/program/` — zero matches (shadowColor '#000' exception does not apply to these files; if found, fix before passing)
    5. Structure check: `ls apps/mobile/app/(tabs)/` still shows exactly 7 entries (_layout + 6 tabs) — no program file leaked into the tab group
    6. Field-name check: `grep -rn "\.name\b" apps/mobile/src/components/program/ProgramDayCard.tsx` — only `day.name` usages, never `exercise.name` (Pitfall 1)

    Update `.planning/phases/04-mobile-program-view/04-VALIDATION.md`: mark per-task rows ✅, check off Wave 0 items, and set the Validation Sign-Off checklist.

    If any check fails: fix within this plan when trivial (< 15 min), otherwise report the gap in the SUMMARY for gap-closure planning.

    Commit (docs + any fixes): `chore(04-03): phase 4 gate — full suites green`
    with Co-Authored-By: Claude Fable 5 &lt;noreply@anthropic.com&gt;
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx jest --silent && cd ../web-admin && npx tsc --noEmit && npx jest --silent</automated>
  </verify>
  <done>Mobile ≥ 209 tests green + tsc clean; web-admin 347 green + tsc clean; zero hardcoded hex in Phase 4 files; tab bar untouched; VALIDATION.md sign-off complete. On-device visual checks (today highlight, navigation feel) are deferred to /gsd:verify-work UAT per VALIDATION.md Manual-Only table — no checkpoint here.</done>
</task>

</tasks>

<verification>
- `grep -n "router.push('/program')" apps/mobile/src/components/home/TodayZone.tsx` — entry link wired
- Full gate: mobile + web-admin suites green, both tsc clean
- `.planning/phases/04-mobile-program-view/04-VALIDATION.md` sign-off checked
</verification>

<success_criteria>
- Program view is reachable from the home screen in both program-present TodayZone branches
- Phase-wide quality bar proven: no test regressions in either app, tsc strict clean, design-token compliance
</success_criteria>

<output>
After completion, create `.planning/phases/04-mobile-program-view/04-03-SUMMARY.md`
</output>
