---
phase: 01-gym-owner-self-serve-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/027_owner_onboarding.sql
  - apps/web-admin/package.json
autonomous: false
requirements: [ONBD-02, ONBD-05, ONBD-06]
user_setup:
  - service: supabase
    why: "Applying migration 027 to the live linked database requires explicit user approval (repo convention)"
    dashboard_config:
      - task: "Approve `npx supabase db push --linked` when the checkpoint fires"
        location: "Terminal (Claude runs the command after approval)"

must_haves:
  truths:
    - "A single RPC call creates gym + membership + settings + billing atomically — a failure partway leaves zero rows"
    - "Inserting a Stripe event_id twice fails on the second insert (unique constraint)"
    - "A members row can be created with onboarding_status = 'invited' without a CHECK violation"
    - "react-hook-form, @hookform/resolvers, and csv-parse are installed in web-admin"
  artifacts:
    - path: "supabase/migrations/027_owner_onboarding.sql"
      provides: "stripe_events_processed table, 'invited' status, complete_gym_onboarding RPC, bulk_import_members RPC"
      contains: "CREATE TABLE stripe_events_processed"
      min_lines: 80
    - path: "apps/web-admin/package.json"
      provides: "react-hook-form, @hookform/resolvers, csv-parse dependencies"
      contains: "react-hook-form"
  key_links:
    - from: "complete_gym_onboarding RPC"
      to: "gyms, gym_memberships, gym_settings, gym_billing tables"
      via: "single plpgsql transaction"
      pattern: "INSERT INTO gym_billing"
    - from: "bulk_import_members RPC"
      to: "members table"
      via: "jsonb array loop with duplicate skip"
      pattern: "onboarding_status.*invited"
---

<objective>
Create the database substrate for the entire onboarding phase (migration 027) and install the three new frontend dependencies.

Purpose: Every other plan in this phase depends on either the RPCs, the idempotency table, the 'invited' status value, or the npm packages. This is the foundation wave.
Output: `supabase/migrations/027_owner_onboarding.sql` applied to the linked Supabase project (`aztppxuapbgmadfigtys`); `react-hook-form`, `@hookform/resolvers`, `csv-parse` installed in web-admin.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-gym-owner-self-serve-onboarding/01-RESEARCH.md
@supabase/migrations/001_nexera_schema.sql

<interfaces>
Verified live schema facts (do not re-derive):

- `gyms`: `slug text UNIQUE NOT NULL`, `owner_id uuid REFERENCES users(id)`, `gym_type` CHECK in ('independent','crossfit','martial-arts','yoga','personal-training','corporate','other'), `subscription_tier` CHECK in ('starter','growth','pro') DEFAULT 'starter', `subscription_status` CHECK in ('trial','active','past_due','cancelled','payment_required') DEFAULT 'trial'
- `gym_memberships`: `role` CHECK in ('owner','trainer','member'), `status` CHECK in ('active','suspended','cancelled','inactive'), UNIQUE(user_id, gym_id)
- `gym_settings`: `gym_id uuid UNIQUE NOT NULL` — all other columns have defaults, so `INSERT INTO gym_settings (gym_id) VALUES (v_gym_id)` works
- `gym_billing`: `gym_id uuid UNIQUE NOT NULL`, `tier text NOT NULL DEFAULT 'starter'`, `subscription_status` CHECK in ('active','trialing','past_due','cancelled','incomplete','incomplete_expired','unpaid','paused') DEFAULT 'trialing', `trial_ends_at timestamptz`
- `members.onboarding_status` current CHECK: ('pending','in_progress','intake_complete','active','program_generating','program_active') — constraint name `members_onboarding_status_check`
- `members` has NO unique constraint on (gym_id, email) or (gym_id, phone) — the import RPC must check duplicates manually
- `members` NOT NULL columns: `gym_id`, `display_name`; `user_id` is nullable
- Repo DB conventions: `gen_random_uuid()` (not uuid_generate_v4), INSERT RLS policies use `WITH CHECK`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 027 and install frontend dependencies</name>
  <files>supabase/migrations/027_owner_onboarding.sql, apps/web-admin/package.json</files>
  <action>
Create `supabase/migrations/027_owner_onboarding.sql` with four sections:

**1. Stripe webhook idempotency table:**
```sql
CREATE TABLE stripe_events_processed (
  event_id     text PRIMARY KEY,
  processed_at timestamptz NOT NULL DEFAULT now()
);
ALTER TABLE stripe_events_processed ENABLE ROW LEVEL SECURITY;
-- No policies: accessed only via service-role admin client (bypasses RLS)
```

**2. Add 'invited' to members.onboarding_status CHECK:**
```sql
ALTER TABLE members DROP CONSTRAINT members_onboarding_status_check;
ALTER TABLE members ADD CONSTRAINT members_onboarding_status_check
  CHECK (onboarding_status IN (
    'pending','in_progress','intake_complete',
    'active','program_generating','program_active','invited'
  ));
```

**3. `complete_gym_onboarding` RPC** — atomic gym creation. Signature: `(p_user_id uuid, p_gym_name text, p_gym_slug text, p_gym_city text DEFAULT NULL, p_gym_type text DEFAULT 'independent', p_tier text DEFAULT 'starter') RETURNS uuid`. Body:
- INSERT INTO gyms (name, slug, owner_id, gym_type, city, subscription_tier, subscription_status) VALUES (..., 'trial') RETURNING id — wrap in a slug-collision loop: on `unique_violation`, retry up to 3 times with `p_gym_slug || '-' || substr(md5(random()::text), 1, 4)`; after 3 failures RAISE
- INSERT INTO gym_memberships (user_id, gym_id, role, status) VALUES (p_user_id, v_gym_id, 'owner', 'active')
- INSERT INTO gym_settings (gym_id) VALUES (v_gym_id)
- INSERT INTO gym_billing (gym_id, tier, subscription_status) VALUES (v_gym_id, p_tier, 'trialing')
- RETURN v_gym_id
- `LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`
- `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated; GRANT EXECUTE ... TO service_role;`

**4. `bulk_import_members` RPC** — signature: `(p_gym_id uuid, p_members jsonb) RETURNS jsonb`. p_members is a jsonb array of objects `{display_name, first_name, email, phone}`. Body:
- Loop over `jsonb_array_elements(p_members)`
- Skip (collect into `v_skipped` jsonb array with `{row_index, reason}`) when: email is non-null AND a members row exists with same gym_id + lower(email) = lower(candidate email) AND is_active = true; OR phone is non-null AND a members row exists with same gym_id + phone AND is_active = true
- Otherwise INSERT INTO members (gym_id, user_id, display_name, first_name, email, phone, onboarding_status) VALUES (p_gym_id, NULL, ..., 'invited'); increment `v_imported`
- RETURN jsonb_build_object('imported', v_imported, 'skipped', coalesce(v_skipped, '[]'::jsonb))
- Because a plpgsql function runs in a single transaction, any raised error rolls back all inserts (all-or-nothing for unexpected failures; duplicates are skipped, not errors)
- Same SECURITY DEFINER + grant pattern as above

Then install dependencies from repo root:
```bash
pnpm add react-hook-form@^7.54.0 @hookform/resolvers@^3.9.0 csv-parse@^5.5.0 --filter web-admin
```
(`@hookform/resolvers ^3.9.0` is the minimum for zod v4 compat — repo has `zod ^4.3.6`.)

Do NOT apply the migration in this task — that is the Task 2 checkpoint.
  </action>
  <verify>
    <automated>cd apps/web-admin && node -e "const p=require('./package.json'); ['react-hook-form','@hookform/resolvers','csv-parse'].forEach(d=>{if(!p.dependencies[d]) throw new Error('missing '+d)}); console.log('deps ok')" && npx jest --silent</automated>
  </verify>
  <done>Migration file exists with all 4 sections; 3 packages in web-admin dependencies; existing 269 web-admin tests still green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Approve and apply migration 027 to the linked database</name>
  <files>none (applies supabase/migrations/027_owner_onboarding.sql to the linked DB)</files>
  <what-built>Migration 027: stripe_events_processed table, 'invited' onboarding status, complete_gym_onboarding RPC, bulk_import_members RPC.</what-built>
  <action>
Pause for user approval (repo convention: db push to the live DB requires explicit approval). After the user replies "approved", run `npx supabase db push --linked` from repo root, then verify the objects exist (see how-to-verify).
  </action>
  <how-to-verify>
Repo convention: `npx supabase db push --linked` against the live DB (project `aztppxuapbgmadfigtys`) requires your explicit approval.

1. Review `supabase/migrations/027_owner_onboarding.sql`
2. Reply "approved" — Claude will then run `npx supabase db push --linked` and verify by executing a read-only check (via Supabase MCP `execute_sql`): `SELECT to_regclass('public.stripe_events_processed'); SELECT proname FROM pg_proc WHERE proname IN ('complete_gym_onboarding','bulk_import_members');` and confirm the CHECK constraint now includes 'invited'.
  </how-to-verify>
  <verify>
    <automated>Supabase MCP execute_sql: SELECT to_regclass('public.stripe_events_processed') IS NOT NULL AS table_ok, (SELECT count(*) FROM pg_proc WHERE proname IN ('complete_gym_onboarding','bulk_import_members')) = 2 AS rpcs_ok</automated>
  </verify>
  <done>User approved; `npx supabase db push --linked` succeeded; table + both RPCs confirmed present; CHECK constraint includes 'invited'.</done>
  <resume-signal>Type "approved" to apply, or describe concerns</resume-signal>
</task>

</tasks>

<verification>
- `supabase/migrations/027_owner_onboarding.sql` exists and is applied to the linked project
- `SELECT complete_gym_onboarding(...)` and `SELECT bulk_import_members(...)` exist in pg_proc
- Web-admin test suite green; `npx tsc --noEmit` clean (no TS files changed, sanity only)
</verification>

<success_criteria>
- All 4 DB objects live in the linked database
- 3 new packages installed; lockfile updated; tests green
</success_criteria>

<output>
After completion, create `.planning/phases/01-gym-owner-self-serve-onboarding/01-01-SUMMARY.md`
</output>
