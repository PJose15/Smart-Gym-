---
phase: 01-gym-owner-self-serve-onboarding
plan: 08
type: execute
wave: 3
depends_on: ["01-01", "01-05"]
files_modified:
  - apps/web-admin/src/lib/import/parseMembersCsv.ts
  - apps/web-admin/src/lib/import/__tests__/parseMembersCsv.test.ts
  - apps/web-admin/src/app/api/owner/members/import/route.ts
  - apps/web-admin/src/app/api/owner/members/__tests__/import.test.ts
  - apps/web-admin/src/app/(onboard)/setup/import/page.tsx
  - apps/web-admin/src/app/api/auth/verify/route.ts
  - apps/web-admin/src/app/api/auth/__tests__/verify-claim.test.ts
autonomous: true
requirements: [ONBD-05, ONBD-06]

must_haves:
  truths:
    - "Owner uploads a real Excel-exported CSV (BOM, quoted fields, header variants) and sees a per-row valid/invalid preview BEFORE anything is written"
    - "Confirming the import writes only clean rows and reports '47 imported, 3 skipped' with a downloadable error report"
    - "Imports over 500 rows are rejected with a clear message"
    - "An imported member (phone in CSV) who signs in on mobile via phone OTP gets linked to their invited row — no duplicate member created"
  artifacts:
    - path: "apps/web-admin/src/lib/import/parseMembersCsv.ts"
      provides: "Pure CSV parse + header normalization + row validation"
      exports: ["parseMembersCsv", "normalizeHeader"]
    - path: "apps/web-admin/src/app/api/owner/members/import/route.ts"
      provides: "Two-phase validate-then-import endpoint"
      exports: ["POST"]
    - path: "apps/web-admin/src/app/(onboard)/setup/import/page.tsx"
      provides: "Upload -> preview -> confirm -> results UI"
      min_lines: 120
  key_links:
    - from: "apps/web-admin/src/app/api/owner/members/import/route.ts"
      to: "bulk_import_members RPC"
      via: "admin.rpc on confirmed import"
      pattern: "bulk_import_members"
    - from: "apps/web-admin/src/app/(onboard)/setup/import/page.tsx"
      to: "/api/owner/members/import"
      via: "FormData POST, validate then import phases"
      pattern: "members/import"
    - from: "apps/web-admin/src/app/api/auth/verify/route.ts"
      to: "invited members"
      via: "findOrCreateMember linking user_id=null rows including 'invited' status"
      pattern: "invited"
---

<objective>
Build the member CSV import (validate-then-import, BOM-safe, 500-row cap) and close the claim loop so imported members link to their row on first mobile phone-OTP login.

Purpose: ONBD-05 + ONBD-06 — success criterion 4. Uses `csv-parse` and the `bulk_import_members` RPC + 'invited' status from plan 01-01.
Output: `/setup/import` page + import API + claim-path fix in `api/auth/verify`.

Scope decision (Claude's discretion, documented): "claim email" sending is deferred — the repo has no transactional-email provider (Supabase only sends auth emails). Claim works via phone OTP (the requirement's actual acceptance path); rows without a phone import fine but are flagged "not claimable until a phone is added". Building an email provider integration is out of scope for this phase.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-gym-owner-self-serve-onboarding/01-RESEARCH.md
@apps/web-admin/src/app/api/auth/verify/route.ts

<interfaces>
**bulk_import_members RPC** (plan 01-01): `admin.rpc('bulk_import_members', { p_gym_id, p_members })` where p_members = jsonb array of `{ display_name, first_name, email, phone }` → returns `{ imported: number, skipped: [{ row_index, reason }] }`. Skips DB-duplicate email/phone per gym; inserts with `onboarding_status: 'invited'`, `user_id: null`.

**findOrCreateMember** (api/auth/verify/route.ts:169, verified): looks up members by `gym_id + phone + is_active`; if found with `user_id === null`, links via update setting `user_id` and mapping `onboarding_status: 'pending' -> 'in_progress'` (other statuses pass through unchanged — 'invited' currently stays 'invited' after linking; Task 3 fixes this).

**verifyStaff('owner')** → `{ user_id, gym_id, admin }` | NextResponse. **checkRateLimit(key, max, ms)**.

**csv-parse sync API:** `import { parse } from 'csv-parse/sync'` → `parse(text, { columns: true, skip_empty_lines: true, trim: true, bom: true })` returns `Record<string,string>[]`.

**Phone normalization:** members are matched by exact `phone` equality in findOrCreateMember. Check how the mobile OTP flow formats phone (E.164, e.g. +15551234567) by reading the verify route's phone source before writing the normalizer — imported phones MUST be stored in the same format or claim matching silently fails. Normalize: strip spaces/dashes/parens; if 10 digits assume US and prefix +1; if starts with + keep; else invalid.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseMembersCsv pure lib + import API route</name>
  <files>apps/web-admin/src/lib/import/parseMembersCsv.ts, apps/web-admin/src/lib/import/__tests__/parseMembersCsv.test.ts, apps/web-admin/src/app/api/owner/members/import/route.ts, apps/web-admin/src/app/api/owner/members/__tests__/import.test.ts</files>
  <behavior>
    - Test 1 (lib): UTF-8 BOM-prefixed CSV parses; first header is 'email' not '﻿email'
    - Test 2 (lib): header aliases map — 'Email Address'/'E-mail' → email; 'Full Name'/'Member Name'/'Name' → display_name; 'Phone Number'/'Mobile'/'Cell' → phone; 'First Name' → first_name
    - Test 3 (lib): row with no display_name but first+last name columns synthesizes display_name; row with neither name → invalid with reason
    - Test 4 (lib): invalid email format → row invalid; missing BOTH email and phone → invalid; valid phone '555-123-4567' normalizes to '+15551234567'
    - Test 5 (lib): in-file duplicate email (case-insensitive) → second row invalid with 'duplicate in file'
    - Test 6 (route): validate phase returns { valid, invalid, total } and performs NO db writes (rpc never called)
    - Test 7 (route): >500 data rows → 400 'Max 500 rows per import'
    - Test 8 (route): import phase calls bulk_import_members with only valid rows and returns { imported, skipped }
    - Test 9 (route): non-owner → 401/403 passthrough
  </behavior>
  <action>
**`lib/import/parseMembersCsv.ts`** (pure, no I/O — fully unit-testable):
```typescript
export interface ParsedMemberRow { display_name: string; first_name: string | null; email: string | null; phone: string | null }
export interface RowError { row: number; errors: string[]; raw: Record<string, string> }
export interface ParseResult { valid: ParsedMemberRow[]; invalid: RowError[]; total: number }
export function parseMembersCsv(text: string): ParseResult
export function normalizeHeader(h: string): string
```
- Strip `﻿` manually first AND pass `bom: true` (belt and braces per Pitfall 4)
- `parse(text, { columns: (headers) => headers.map(normalizeHeader), skip_empty_lines: true, trim: true, bom: true, relax_column_count: true })`
- normalizeHeader: lowercase, trim, spaces→underscore, then alias table (email variants, name variants, phone variants, first/last name)
- Row validation: display_name from `display_name` || `${first_name} ${last_name}`.trim() — required; email optional but must match zod `.email()` when present (lowercase it); phone optional, normalize per interfaces note; require email OR phone; flag phone-less rows as valid but with `warning: 'no phone — member cannot claim via mobile until one is added'` (add optional `warnings: string[]` to ParsedMemberRow)
- Case-insensitive in-file email dedup + exact phone dedup
- Wrap `parse()` in try/catch → malformed CSV returns everything-invalid with a single parse error

**`api/owner/members/import/route.ts`** POST:
1. `verifyStaff('owner')`; `checkRateLimit(\`members-import:${user_id}\`, 10, 600_000)`
2. `formData()` → `file` (File, reject > 1 MB with 400) + `mode` field: `'validate' | 'import'`
3. `parseMembersCsv(await file.text())`; enforce `total <= 500` (400 otherwise)
4. mode validate → return `{ valid, invalid, total }` (include row warnings) — zero DB access
5. mode import → require `invalid.length === 0`? NO — import only `valid` rows (owner confirmed after seeing the report); call `admin.rpc('bulk_import_members', { p_gym_id: gym_id, p_members: valid })`; RPC error → 500; return `{ imported, skipped, invalid_count: invalid.length }`
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="parseMembersCsv|members.*import" --no-coverage --silent</automated>
  </verify>
  <done>9 behaviors green; validate phase provably write-free; Excel BOM + header variants handled.</done>
</task>

<task type="auto">
  <name>Task 2: /setup/import page (upload → preview → confirm → results)</name>
  <files>apps/web-admin/src/app/(onboard)/setup/import/page.tsx</files>
  <action>
Client component, four UI states:

**1. Upload:** `<WizardProgress currentStep={4} />`, heading "Import your members", drag-drop zone + file input (`accept=".csv"`), helper "Export from Excel/Sheets as CSV. Columns we understand: Name, Email, Phone. Up to 500 members per file." On file select → POST FormData `{ file, mode: 'validate' }` to `/api/owner/members/import`.

**2. Preview:** summary chips "{valid} ready · {invalid} with errors · {warnings} without phone" (green/red/amber tokens). Table of first 10 valid rows (Name/Email/Phone). If invalid rows: collapsible error list ("Row 12: invalid email 'bob@'"), and a "Download error report" button generating a client-side CSV blob (`row,errors,original_data`) via `URL.createObjectURL` — no server round-trip. Buttons: "Import {valid} members" (primary, disabled when valid=0) and "Choose a different file".

**3. Importing:** spinner. POST `{ file, mode: 'import' }` (re-send the same File object).

**4. Results:** "✅ {imported} members imported" + "{skipped.length} skipped (already in your gym)" with reasons list when non-empty, matching the ONBD-05 report format. Note under results: "Members sign in on the mobile app with their phone number to claim their profile." CTAs: "Go to my dashboard" → `/owner/dashboard`, "Import another file" resets.

Error handling: 400/413/500 → inline error banner, stay on current state. DOC_03 tokens throughout. This page must also be reachable for existing owners — it lives under (onboard) but requires only a session (same guard as /setup: fetch `/api/onboard/status`, 401 → sign-in prompt).
  </action>
  <verify>
    <automated>cd apps/web-admin && npx tsc --noEmit</automated>
  </verify>
  <done>Full validate-then-import UX with per-row report and error-report download; no writes until explicit confirm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Claim path — link 'invited' members on phone OTP</name>
  <files>apps/web-admin/src/app/api/auth/verify/route.ts, apps/web-admin/src/app/api/auth/__tests__/verify-claim.test.ts</files>
  <behavior>
    - Test 1: member row with user_id=null, onboarding_status='invited', matching phone → after verify, user_id set AND onboarding_status becomes 'in_progress' (member proceeds through normal mobile onboarding/intake)
    - Test 2: 'pending' → 'in_progress' mapping still works (regression)
    - Test 3: already-linked member (user_id set) untouched by claim logic
  </behavior>
  <action>
In `findOrCreateMember` (api/auth/verify/route.ts ~line 190), change the status mapping in the linking update from:
```typescript
onboarding_status: existing.onboarding_status === 'pending' ? 'in_progress' : existing.onboarding_status
```
to treat `'invited'` the same as `'pending'`:
```typescript
onboarding_status: (existing.onboarding_status === 'pending' || existing.onboarding_status === 'invited')
  ? 'in_progress' : existing.onboarding_status
```
No other changes to the route — the phone match + link mechanics already work (verified). Write the test file mocking the admin client per the route's existing test patterns (check for an existing verify route test to extend rather than duplicate; if `coaching.test.ts`-style helpers exist for this route, reuse them).
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="verify-claim" --no-coverage --silent && npx jest --silent</automated>
  </verify>
  <done>Invited members transition to the normal onboarding funnel on first OTP login; full suite green.</done>
</task>

</tasks>

<verification>
- Full web-admin suite green; tsc clean
- Real-device OTP claim + real Excel CSV deferred to plan 01-09 checkpoint
</verification>

<success_criteria>
- Validate-then-import with per-row report, BOM safety, 500-row cap
- Phone-OTP claim links invited rows; no duplicate members
</success_criteria>

<output>
After completion, create `.planning/phases/01-gym-owner-self-serve-onboarding/01-08-SUMMARY.md`
</output>
