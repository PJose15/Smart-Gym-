---
phase: 01-gym-owner-self-serve-onboarding
plan: 06
type: execute
wave: 3
depends_on: ["01-02", "01-05"]
files_modified:
  - apps/web-admin/src/app/api/billing/checkout/route.ts
  - apps/web-admin/src/lib/validation/staff.ts
  - apps/web-admin/src/app/api/billing/prices/route.ts
  - apps/web-admin/src/app/api/billing/__tests__/checkout-context.test.ts
  - apps/web-admin/src/app/(onboard)/subscribe/page.tsx
  - apps/web-admin/src/app/(onboard)/components/TierCard.tsx
autonomous: true
requirements: [ONBD-01, ONBD-03]

must_haves:
  truths:
    - "Clicking the email verification link lands the owner on /subscribe with a live session (code exchanged) and their gym context loaded"
    - "Owner picks a tier and is redirected to Stripe Checkout; completing it returns to /setup, cancelling returns to /subscribe"
    - "An owner who abandoned checkout sees a 'Complete your subscription' banner and can resume — no duplicate-customer creation"
    - "Tier cards show real prices fetched from Stripe, not hardcoded guesses"
  artifacts:
    - path: "apps/web-admin/src/app/(onboard)/subscribe/page.tsx"
      provides: "Step 3: code exchange, session refresh, tier picker, checkout redirect, resume banner"
      min_lines: 100
    - path: "apps/web-admin/src/app/api/billing/prices/route.ts"
      provides: "GET live tier prices from Stripe (cached)"
      exports: ["GET"]
    - path: "apps/web-admin/src/app/api/billing/checkout/route.ts"
      provides: "context-aware checkout with onboarding success/cancel URLs"
      contains: "onboarding"
  key_links:
    - from: "apps/web-admin/src/app/(onboard)/subscribe/page.tsx"
      to: "supabase.auth.exchangeCodeForSession + refreshSession"
      via: "on-mount ?code handling (Pitfall 3)"
      pattern: "exchangeCodeForSession"
    - from: "apps/web-admin/src/app/(onboard)/subscribe/page.tsx"
      to: "/api/billing/checkout"
      via: "POST { tier, interval, context: 'onboarding' } then redirect to checkout_url"
      pattern: "api/billing/checkout"
    - from: "apps/web-admin/src/app/api/billing/checkout/route.ts"
      to: "createCheckoutSession urls param"
      via: "context === 'onboarding' -> /setup and /subscribe URLs"
      pattern: "successUrl"
---

<objective>
Build wizard Step 3: the subscribe page (tier picker with live Stripe prices), the email-link session handshake, and the abandoned-checkout resume path.

Purpose: ONBD-01 (plan selection + checkout step) and ONBD-03 (abandonment shows "complete your subscription", retry-safe). Uses the `urls` parameter added to `createCheckoutSession` in plan 01-02 and the `/api/onboard/status` route from plan 01-05.
Output: Verified-email owner can pick Starter/Growth/Pro and reach Stripe Checkout; abandoners can resume.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-gym-owner-self-serve-onboarding/01-RESEARCH.md
@apps/web-admin/src/app/api/billing/checkout/route.ts
@apps/web-admin/src/lib/billing/stripeHelpers.ts

<interfaces>
**checkout route today** (verified): `verifyStaff('owner')` → `checkoutSchema` (lib/validation/staff.ts:112, fields tier + interval) → rate limit → get-or-create Stripe customer (reuses `gym_billing.stripe_customer_id` — Pitfall 6 already handled on the customer side) → `createCheckoutSession(gym_id, customerId, tier, interval)` → `{ checkout_url }`.

**createCheckoutSession after plan 01-02:**
```typescript
createCheckoutSession(gymId, customerId, tier, interval, urls?: { successUrl?: string; cancelUrl?: string })
```

**getPriceId(tier, interval)** maps to env vars `STRIPE_PRICE_{TIER}_{INTERVAL}` (already configured — pre-GSD Phase 7). `getStripe()` from `@/lib/billing/stripeClient`.

**GET /api/onboard/status** (plan 01-05) returns `{ gym_id, gym_name, tier, email_confirmed, billing: { has_customer, has_subscription, subscription_status } }` — 401 without session.

**Session handshake:** signUp used `emailRedirectTo: ${appUrl}/subscribe`. With the @supabase/ssr browser client (PKCE), the verification link redirects to `/subscribe?code=...`; the page must call `supabase.auth.exchangeCodeForSession(window.location.href)` (or the code string, per installed supabase-js version — check its signature) then `supabase.auth.refreshSession()` before fetching status (Pitfall 3: membership row was created after any prior token was issued).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Context-aware checkout route + live prices endpoint</name>
  <files>apps/web-admin/src/lib/validation/staff.ts, apps/web-admin/src/app/api/billing/checkout/route.ts, apps/web-admin/src/app/api/billing/prices/route.ts, apps/web-admin/src/app/api/billing/__tests__/checkout-context.test.ts</files>
  <behavior>
    - Test 1: POST checkout with `context: 'onboarding'` → createCheckoutSession called with urls `{ successUrl: `${appUrl}/setup?checkout=success&session_id={CHECKOUT_SESSION_ID}`, cancelUrl: `${appUrl}/subscribe?cancelled=true` }`
    - Test 2: POST without context → createCheckoutSession called with NO urls arg (undefined) — existing behavior preserved
    - Test 3: invalid context value rejected by schema (400)
    - Test 4 (prices): GET returns `{ prices: { starter: { monthly: {...}, annual: {...} }, growth: ..., pro: ... } }` with `unit_amount` + `currency` from mocked stripe.prices.retrieve; second call within TTL does not re-hit Stripe (module-level cache)
  </behavior>
  <action>
**`lib/validation/staff.ts`**: extend `checkoutSchema` with `context: z.enum(['onboarding']).optional()`.

**checkout route**: after parsing, build `urls` only when `parsed.data.context === 'onboarding'` (values per Test 1, `appUrl` from `NEXT_PUBLIC_APP_URL`); pass as 5th arg.

**`api/billing/prices/route.ts`** GET (session required — reuse the light session check pattern from `/api/onboard/status`, NOT verifyStaff, since it's harmless read-only data; rate limit `billing-prices:${ip}` 30/min):
- For each of the 6 (tier, interval) combos: `getPriceId(tier, interval)` → `stripe.prices.retrieve(priceId)` → `{ unit_amount, currency, interval }`
- Wrap in try/catch per price: if an env var is missing or retrieval fails, set that combo to `null` (page renders "—" and still works in dev without Stripe env)
- Module-level in-memory cache `{ data, fetchedAt }` with 1-hour TTL
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="checkout-context" --no-coverage --silent</automated>
  </verify>
  <done>Onboarding checkouts round-trip to /setup and /subscribe; default flow untouched; prices served from Stripe with cache.</done>
</task>

<task type="auto">
  <name>Task 2: Subscribe page (Step 3) with session handshake and resume banner</name>
  <files>apps/web-admin/src/app/(onboard)/subscribe/page.tsx, apps/web-admin/src/app/(onboard)/components/TierCard.tsx</files>
  <action>
**TierCard.tsx**: props `{ tier: 'starter'|'growth'|'pro', name, blurb, features: string[], price: { unit_amount, currency } | null, interval, highlighted?, onSelect, disabled }`. DOC_03 card styling; `highlighted` (Growth) gets `var(--accent)` border + "Most popular" pill. Price rendered as `$X/mo` (unit_amount/100) or "—" when null. Feature bullets: keep copy short — Starter: "Up to 100 members, QR machine flow, member app"; Growth: "Everything in Starter + AI coaching, challenges, automations"; Pro: "Everything in Growth + white-label, priority support". (Copy is display-only; adjust freely to match existing tier language found in `owner/billing` page if present — check it first.)

**subscribe/page.tsx** (client, wrapped in Suspense for useSearchParams):
1. On mount, in order:
   a. If `?code` param present: `supabase.auth.exchangeCodeForSession(...)` (browser client from `@/lib/supabase`; confirm exact signature against installed version), then strip the param via `router.replace('/subscribe')`
   b. `supabase.auth.refreshSession()` — Pitfall 3, always
   c. Fetch `/api/onboard/status`; 401 → render "Session expired" state with link to `/signup`; 404 no_gym → link to `/signup`
2. Render `<WizardProgress currentStep={3} />`, heading "Choose your plan — 30 days free", monthly/annual toggle, 3 TierCards from `/api/billing/prices`
3. **Resume banner** (Pitfall 6): if `status.billing.has_customer && !status.billing.has_subscription && searchParams has cancelled=true` OR status shows a prior attempt (`has_customer` true) — show banner "Your subscription isn't finished — pick a plan to complete setup" (amber/accent tinted, DOC_03 tokens). If `has_subscription` is already true, skip this page entirely: `router.replace('/setup')`
4. Selecting a tier: POST `/api/billing/checkout` `{ tier, interval, context: 'onboarding' }` → `window.location.assign(checkout_url)`; button spinner + disable all cards while pending; error banner on failure
  </action>
  <verify>
    <automated>cd apps/web-admin && npx tsc --noEmit && npx jest --silent</automated>
  </verify>
  <done>Email link → live session → tier picker → Stripe redirect works; already-subscribed and abandoned states both route correctly; suite + tsc green.</done>
</task>

</tasks>

<verification>
- Full web-admin suite green; tsc clean
- Stripe test-mode end-to-end deferred to plan 01-09 checkpoint (needs `stripe listen`)
</verification>

<success_criteria>
- Step 3 complete: verified owner reaches Stripe Checkout with correct return URLs
- Abandonment is resumable with no duplicate Stripe customers
</success_criteria>

<output>
After completion, create `.planning/phases/01-gym-owner-self-serve-onboarding/01-06-SUMMARY.md`
</output>
