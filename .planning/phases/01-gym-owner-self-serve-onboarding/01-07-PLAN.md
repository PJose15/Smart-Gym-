---
phase: 01-gym-owner-self-serve-onboarding
plan: 07
type: execute
wave: 3
depends_on: ["01-03", "01-05"]
files_modified:
  - apps/web-admin/src/app/(onboard)/setup/page.tsx
  - apps/web-admin/src/app/(onboard)/setup/__tests__/setup-wizard.test.tsx
autonomous: true
requirements: [ONBD-04]

must_haves:
  truths:
    - "A just-subscribed owner lands on /setup and creates their first machine in a guided 3-step flow"
    - "The final step offers a downloadable printable QR PDF plus the machine's scan URL"
    - "Owner can skip machine creation and still proceed (to import or dashboard) — no dead ends"
  artifacts:
    - path: "apps/web-admin/src/app/(onboard)/setup/page.tsx"
      provides: "Step 4: first-machine wizard (form -> create -> QR download)"
      min_lines: 100
  key_links:
    - from: "apps/web-admin/src/app/(onboard)/setup/page.tsx"
      to: "POST /api/machines"
      via: "MachineForm minimal onSubmit"
      pattern: "api/machines"
    - from: "apps/web-admin/src/app/(onboard)/setup/page.tsx"
      to: "GET /api/machines/qr-pdf"
      via: "download link on success step"
      pattern: "qr-pdf"
---

<objective>
Build wizard Step 4: the first-machine guided flow inside `(onboard)/setup` — minimal machine form → server-side creation → QR PDF download with next-step CTAs.

Purpose: ONBD-04 — success criterion 3: "owner creates their first machine through the guided wizard and downloads a printable QR PDF that scans into the member flow." Consumes `MachineForm` (minimal mode) and `POST /api/machines` from plan 01-03, and the `(onboard)/` shell + `/api/onboard/status` from plan 01-05.
Output: Working /setup wizard page.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-gym-owner-self-serve-onboarding/01-RESEARCH.md
@apps/web-admin/src/app/api/machines/qr-pdf/route.ts
@apps/web-admin/src/components/machines/MachineForm.tsx

<interfaces>
**From plan 01-03:**
- `MachineForm` with `minimal` prop: renders name + muscle checkbox grid + equipment type; `onSubmit(values: MachineFormValues)`
- `POST /api/machines` body `{ name, target_muscles, equipment_type?, ... }` → 201 `{ id, name, qr_slug }`; 401/403 when unauthenticated

**Existing QR PDF route** (verified production-ready): `GET /api/machines/qr-pdf` uses `generateQrPdf(machines, gymName, baseUrl)` and returns an `application/pdf` byte stream of all the gym's machine QR codes. Read the route before wiring to confirm auth + query params (it may accept a gym filter — the wizard just needs the current owner's gym default).

**From plan 01-05:** `GET /api/onboard/status` → 401 without session; returns `{ gym_id, gym_name, billing: { has_subscription, ... } }`. `WizardProgress` at `(onboard)/components/WizardProgress.tsx`.

**Member scan URL shape:** `/m/[qr_slug]` (existing machine landing route).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /setup first-machine wizard page</name>
  <files>apps/web-admin/src/app/(onboard)/setup/page.tsx</files>
  <action>
Client component (Suspense-wrapped for `useSearchParams` — reads `?checkout=success`), internal step state machine `'machine-form' | 'creating' | 'qr-ready'`:

**Guard:** on mount fetch `/api/onboard/status`; 401 → render sign-in prompt linking `/staff/login?next=/setup`; success → store `gym_name`. If `?checkout=success`, show a one-time success toast/banner "Subscription active — your 30-day trial has started" (dismissable).

**Step A (machine-form):** `<WizardProgress currentStep={4} />`, heading "Add your first machine", sub "Name it like your members would say it — 'Lat Pulldown', 'Leg Press'". Render `<MachineForm minimal submitLabel="Create machine & QR code" ... />`. On submit → POST `/api/machines`; move to `creating` (spinner) → on 201 store `{ id, name, qr_slug }` → `qr-ready`. On error: return to form with error message displayed.

**Step B (qr-ready):** celebration-style success panel (use existing DOC_03 celebration/confetti component if one fits — check `components/` for the celebration components noted in STATE.md before hand-rolling):
- "{name} is live!" + the scan URL `{origin}/m/{qr_slug}` in a copyable code block (copy button)
- Primary button "Download printable QR PDF" → anchor to `/api/machines/qr-pdf` with `download` attribute
- Secondary CTA "Import your members →" → `/setup/import`
- Tertiary link "Go to my dashboard" → `/owner/dashboard`
- Link "Add another machine" resets to Step A

**Skip path:** on Step A, a muted "Skip for now" link → `/setup/import`. Never trap the owner.

All styling via DOC_03 tokens; reuse the (onboard) shell spacing from signup page.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx tsc --noEmit</automated>
  </verify>
  <done>Wizard flows form → create → QR download; skip and error paths work; no dead ends.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wizard flow tests</name>
  <files>apps/web-admin/src/app/(onboard)/setup/__tests__/setup-wizard.test.tsx</files>
  <behavior>
    - Test 1: 401 from /api/onboard/status → sign-in prompt rendered, no form
    - Test 2: happy path — status ok, submit MachineForm → POST /api/machines called with entered name + muscles → QR step shows machine name, scan URL containing qr_slug, and a link whose href is /api/machines/qr-pdf
    - Test 3: POST failure (500) → form re-rendered with error message
    - Test 4: "Skip for now" link present and points to /setup/import
  </behavior>
  <action>
React Testing Library tests mocking global fetch (per existing web-admin page-test patterns — find an existing page test using mocked fetch + next/navigation mocks and mirror it). Mock `next/navigation` (useRouter, useSearchParams). Assert the four behaviors.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="setup-wizard" --no-coverage --silent && npx jest --silent</automated>
  </verify>
  <done>4 behaviors green; full suite green.</done>
</task>

</tasks>

<verification>
- Full web-admin suite green; tsc clean
- Physical QR scan verification deferred to plan 01-09 checkpoint (manual by design)
</verification>

<success_criteria>
- Zero-machine gym → named machine → downloadable QR PDF, entirely inside the wizard
- Skip/error paths leave no stuck state
</success_criteria>

<output>
After completion, create `.planning/phases/01-gym-owner-self-serve-onboarding/01-07-SUMMARY.md`
</output>
