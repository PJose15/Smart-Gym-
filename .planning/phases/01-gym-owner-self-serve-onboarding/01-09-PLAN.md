---
phase: 01-gym-owner-self-serve-onboarding
plan: 09
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05", "01-06", "01-07", "01-08"]
files_modified: []
autonomous: false
requirements: [ONBD-01, ONBD-02, ONBD-03, ONBD-04, ONBD-05, ONBD-06, ONBD-07]
user_setup:
  - service: stripe
    why: "End-to-end checkout verification needs test-mode webhooks forwarded locally"
    dashboard_config:
      - task: "Have the Stripe CLI installed and logged in (stripe login) for `stripe listen`"
        location: "Local terminal"
    env_vars:
      - name: STRIPE_WEBHOOK_SECRET
        source: "Printed by `stripe listen` at startup (whsec_...) — must match .env.local during the test"

must_haves:
  truths:
    - "The complete journey — landing → signup → email verify → checkout → machine QR → CSV import → dashboard checklist — works with zero human admin intervention"
    - "CI-equivalent gates are green: tsc strict, ESLint, full web-admin jest suite"
    - "Webhook replays and checkout abandonment leave no stuck or duplicated state"
  artifacts: []
  key_links: []
---

<objective>
Phase-gate verification: run every automated gate, then walk the full onboarding journey end-to-end in Stripe test mode against the dev environment.

Purpose: The phase's success criteria are journey-level truths that only a real walkthrough can prove (Stripe checkout, physical QR scan, mobile OTP claim). This plan is the single consolidated human checkpoint — earlier plans deliberately deferred manual verification here to avoid checkpoint fatigue.
Output: Verified phase, or a concrete gap list for `/gsd:plan-phase 1 --gaps`.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-gym-owner-self-serve-onboarding/01-VALIDATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated gate</name>
  <files>none (verification only)</files>
  <action>
Run, in order, from repo root:
1. `cd apps/web-admin && npx tsc --noEmit`
2. `cd apps/web-admin && npx eslint src --max-warnings=0` (match the repo's CI lint invocation — check `.github/workflows` or package.json scripts for the exact command and use that instead if it differs)
3. `cd apps/web-admin && npx jest --silent` (baseline 269 + all new phase tests)
4. `cd apps/mobile && npx jest --silent` (regression only — phase touched no mobile code; 135 baseline)
5. `pnpm -F @nexera/ai-assist test -- --run` (regression only; 399 baseline)

Fix any failures caused by this phase's code before proceeding. Report final counts.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx tsc --noEmit && npx jest --silent</automated>
  </verify>
  <done>All suites green, tsc strict clean, lint clean. Total ≥ 803 tests passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end onboarding walkthrough</name>
  <files>none (manual verification)</files>
  <action>
Present the walkthrough below to the user and pause. Before pausing, ensure the dev server starts cleanly (`cd apps/web-admin && pnpm dev` boots without errors) so the user's time is not wasted on a broken build.
  </action>
  <what-built>The complete gym-owner self-serve onboarding: 4-step wizard, Stripe trial checkout with idempotent webhooks, first-machine QR wizard, CSV member import with phone-OTP claim, and dashboard checklist + trial countdown.</what-built>
  <how-to-verify>
Setup (two terminals):
- T1: `cd apps/web-admin && pnpm dev`
- T2: `stripe listen --forward-to localhost:3000/api/billing/webhook` (copy the whsec_ into .env.local as STRIPE_WEBHOOK_SECRET, restart dev server if changed)

Walkthrough:
1. **Signup (ONBD-01/02):** Open http://localhost:3000/signup in a private window. Create an account with a real email you control + a new gym name. Expect: progress indicator on step 1, validation errors on bad input, then redirect to the verify-email screen showing your address.
2. **Email verify:** Click the link in the inbox. Expect: you land on /subscribe with tier cards showing real prices (step 3 highlighted) — no login prompt, no empty screen.
3. **Checkout (ONBD-03):** Pick Growth monthly → Stripe Checkout. Pay with 4242 4242 4242 4242. Expect: redirect to /setup with "Subscription active" banner; T2 shows `checkout.session.completed` → 200. In Stripe dashboard: subscription is `trialing` with payment method attached.
4. **Abandonment resume (ONBD-03):** In a second private window, sign up a second gym, reach Stripe Checkout, and close the tab. Return to /subscribe. Expect: "Complete your subscription" banner; retrying reuses the same customer (Stripe dashboard shows ONE customer for that gym).
5. **Webhook idempotency (ONBD-03):** In T2 press Ctrl+C, rerun `stripe listen`, then `stripe events resend <id-of-the-completed-event>`. Expect: 200 response, and server logs show action 'duplicate' — gym_billing unchanged.
6. **Machine wizard (ONBD-04):** On /setup, create "Lat Pulldown" with 2 muscle groups. Expect: success panel with scan URL; download the QR PDF; scan the QR with your phone camera → the machine landing page loads.
7. **CSV import (ONBD-05):** Create a small CSV in Excel/Sheets (columns: Name, Email, Phone — include your own phone on one row, one bad email row, one duplicate row) and upload at /setup/import. Expect: preview shows correct valid/invalid split with row-level errors + downloadable error report; confirm → "N imported, M skipped" report.
8. **Member claim (ONBD-06):** On the mobile app (Expo dev build), sign in with the phone number from your CSV against this gym. Expect: you land in the member flow linked to the imported row (name pre-filled from import), and the web members list shows the row moved from 'invited' — no duplicate member row created.
9. **Dashboard (ONBD-07):** Open /owner/dashboard as the new owner. Expect: trial countdown banner ("30 days left" ±1) and setup checklist with "Add machine" ✓, "Import members" ✓, "Share QR" per actual scan state. After step 6's scan, the QR item should show complete on refresh.
  </how-to-verify>
  <verify>
    <automated>MANUAL — journey-level verification (Stripe checkout, physical QR scan, mobile OTP) cannot be automated; automated gates ran in Task 1</automated>
  </verify>
  <done>User confirms all 9 walkthrough steps pass; any failures recorded as gaps with step numbers.</done>
  <resume-signal>Type "approved" if all 9 steps pass, or list the step numbers that failed with what you saw</resume-signal>
</task>

</tasks>

<verification>
- All automated gates green
- All 9 walkthrough steps approved by user
</verification>

<success_criteria>
- Phase 1 success criteria 1-5 (ROADMAP.md) observed live
- Any failures converted into a concrete gap list for `/gsd:plan-phase 1 --gaps`
</success_criteria>

<output>
After completion, create `.planning/phases/01-gym-owner-self-serve-onboarding/01-09-SUMMARY.md`
</output>
