---
phase: 06-notification-orchestration
plan: 09
type: execute
wave: 3
depends_on: ["06-01", "06-08"]
files_modified:
  - apps/mobile/src/lib/notificationInboxService.ts
  - apps/mobile/src/lib/__tests__/notificationInboxService.test.ts
  - apps/mobile/src/hooks/useUnreadNotifications.ts
  - apps/mobile/app/notifications/index.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/settings.tsx
autonomous: true
requirements: [NOTIF-04, NOTIF-05]
must_haves:
  truths:
    - "Member opens /notifications and sees history newest-first with unread items visually distinct; tapping marks read and deep-links via resolveNotificationRoute"
    - "The home-header bell shows an unread badge and routes to /notifications (replacing the /coach-notes link)"
    - "Member can toggle each push category (7 booleans, grouped Activity/Coaching/Social) and set quiet hours from settings — persisted to notification_preferences keyed by member_id"
    - "Quiet hours UI notes times are enforced in UTC (documented MVP limitation, Pitfall 7)"
  artifacts:
    - path: "apps/mobile/src/lib/notificationInboxService.ts"
      provides: "fetchInbox(cursor?) + markRead(id) against the 06-08 API with Bearer JWT"
      exports: ["fetchInbox", "markRead"]
    - path: "apps/mobile/src/hooks/useUnreadNotifications.ts"
      provides: "unread count from API + markInboxViewed module-listener reset (mirrors useUnreadFeedCount)"
      exports: ["useUnreadNotifications", "markInboxViewed"]
    - path: "apps/mobile/app/notifications/index.tsx"
      provides: "inbox screen: list, unread styling, tap-to-read+navigate, empty state, skeleton"
      min_lines: 100
    - path: "apps/mobile/app/settings.tsx"
      provides: "per-category push toggles + quiet hours window UI"
      contains: "quiet_hours_enabled"
  key_links:
    - from: "apps/mobile/app/notifications/index.tsx"
      to: "apps/mobile/src/lib/notificationService.ts"
      via: "resolveNotificationRoute(notification_type, data) on tap"
      pattern: "resolveNotificationRoute"
    - from: "apps/mobile/app/(tabs)/_layout.tsx"
      to: "/notifications"
      via: "bell headerRight router.push + useUnreadNotifications badge"
      pattern: "useUnreadNotifications"
    - from: "apps/mobile/app/settings.tsx"
      to: "notification_preferences"
      via: "supabase upsert onConflict member_id (RLS notification_prefs_own)"
      pattern: "notification_preferences"
---

<objective>
Build the mobile surface: notification inbox screen with unread badge on the home-header bell, and the notification preferences UI (per-category toggles + quiet hours) in settings.

Purpose: NOTIF-05 (inbox + unread badge + read sync) and NOTIF-04's UI half (server-side enforcement shipped in 06-03; the schema needs zero changes). Reuses the feed-tab badge pattern and the Phase 3 screen conventions.
Output: inbox service + hook + screen, bell rewire, settings preferences section.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-notification-orchestration/06-RESEARCH.md
@.planning/phases/06-notification-orchestration/06-08-SUMMARY.md
@apps/mobile/src/hooks/useUnreadFeedCount.ts
@apps/mobile/src/lib/challengeService.ts
@apps/mobile/app/(tabs)/_layout.tsx
@apps/mobile/app/settings.tsx
</context>

<interfaces>
Inbox API contract (06-08): GET `/api/member/notifications?member_id=&cursor=&limit=` → `{ notifications: [{ id, notification_type, title, body, data, read_at, created_at }], unread_count, next_cursor }`; POST `/api/member/notifications/[id]/read` body `{ member_id }` → `{ success: true }`. Base URL + Bearer JWT + member_id resolution: copy challengeService.ts conventions exactly (EXPO_PUBLIC_API_URL, session access token, resolveMemberInfo/getMemberId helper reuse — check what challengeService/feedService import).

Deep-link resolver (06-01): `import { resolveNotificationRoute } from '../src/lib/notificationService'` — returns route string or null; cast push via `router.push(path as Href)` (repo convention for dynamic routes; Expo typed-routes stale, as-any cast precedent documented in STATE.md).

Design system: DOC_18 dark tokens from src/theme/colors.ts ONLY — zero hardcoded hex (phase gate greps for it). Existing screen conventions: skeleton loaders, empty states, Text/AnimatedCard components from src/components.

Settings prefs (already correct in settings.tsx global toggle): supabase.from('notification_preferences').upsert({ member_id, ... }, { onConflict: 'member_id' }) under RLS policy notification_prefs_own. Category columns: push_prs, push_achievements, push_level_up, push_challenge_rank (group "Activity"); push_trainer_note, push_new_program (group "Coaching"); push_gym_feed (group "Social"); plus quiet_hours_enabled, quiet_hours_start, quiet_hours_end (time strings 'HH:MM:SS').
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inbox service + unread hook</name>
  <files>apps/mobile/src/lib/notificationInboxService.ts, apps/mobile/src/lib/__tests__/notificationInboxService.test.ts, apps/mobile/src/hooks/useUnreadNotifications.ts</files>
  <behavior>
    - fetchInbox(cursor?) returns typed { notifications, unread_count, next_cursor }; API 4xx/5xx or network error → returns null (graceful, matches fetchChallengeDetail convention); attaches Bearer token + member_id
    - markRead(id) POSTs and resolves boolean success; never throws
    - Tests mock ../supabase (session + member lookup) and global fetch; follow challengeService.test.ts mock conventions
  </behavior>
  <action>
RED first for the service. Then useUnreadNotifications hook modeled directly on useUnreadFeedCount's module-listener pattern: state from fetchInbox().unread_count on mount/refresh; exported markInboxViewed() zeroes every mounted badge (no AsyncStorage needed — read_at is server-truth, so refresh simply refetches); no realtime subscription (notifications table is not in the realtime publication — poll on focus is enough for v1, note it in a comment). Hook itself is covered indirectly (service tests + tsc), matching useUnreadFeedCount precedent.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest notificationInboxService --silent</automated>
  </verify>
  <done>Service tested green; hook mirrors the feed-badge pattern; mobile tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Inbox screen + bell rewire</name>
  <files>apps/mobile/app/notifications/index.tsx, apps/mobile/app/(tabs)/_layout.tsx</files>
  <action>
1. Create app/notifications/index.tsx (Stack route like app/program/index.tsx — verify registration needs in app/_layout.tsx): FlatList of notifications; each row: title, body, relative timestamp, unread = accent left-border + brighter title (DOC_18 tokens); tap → markRead(id) optimistic (flip local read_at), call markInboxViewed-refresh, then resolveNotificationRoute(notification_type as NotificationType, data) → router.push when non-null, no-op when null; pagination via next_cursor onEndReached; pull-to-refresh; skeleton on first load; empty state ("No notifications yet — go crush a workout"); a11y labels on rows (repo a11y conventions).
2. (tabs)/_layout.tsx: change the home headerRight bell onPress from router.push('/coach-notes') to router.push('/notifications'); wrap the bell with an unread badge using useUnreadNotifications (reuse the exact badge visual used for the feed tab / gold-dot pattern already in this file — inspect and match it).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx jest --silent</automated>
  </verify>
  <done>Inbox screen renders history with unread styling, tap marks read + deep-links; bell badges and routes to /notifications; zero hardcoded hex.</done>
</task>

<task type="auto">
  <name>Task 3: Notification preferences UI in settings</name>
  <files>apps/mobile/app/settings.tsx</files>
  <action>
Extend the existing notification section in settings.tsx (keep the global On/Off toggle exactly as-is — it writes `enabled`):
1. On load (inside the existing prefs fetch), select all 7 push category columns + 3 quiet-hours columns alongside `enabled` (single query, member_id keyed).
2. Render grouped toggles under the global toggle, disabled/dimmed when global is Off: Activity (PRs → push_prs, Achievements & streaks → push_achievements, Level ups → push_level_up, Challenges & leaderboard → push_challenge_rank), Coaching (Trainer notes & check-ins → push_trainer_note, New programs → push_new_program), Social (Gym feed activity → push_gym_feed).
3. Quiet hours: enable toggle + start/end time selection. Keep the control simple — reuse the existing toggle-row visual language; for time entry use a preset picker (e.g. hour steps rendered as horizontal chips or a simple modal list — match existing settings UI primitives, no new deps). Persist as 'HH:MM:SS'. Include helper text: "Quiet hours use UTC for now" (Pitfall 7 MVP limitation).
4. Each change upserts optimistically with rollback-on-error, mirroring handleToggleNotifications. KeyboardAvoidingView/a11y conventions of the file preserved.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx jest --silent</automated>
  </verify>
  <done>All 7 category toggles + quiet hours persist to notification_preferences; UTC note visible; existing settings behavior untouched.</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx tsc --noEmit && npx jest --silent` — clean, 227+ green
- Hex scan on new/changed mobile files: `grep -nE "#[0-9a-fA-F]{3,8}" apps/mobile/app/notifications/index.tsx apps/mobile/src/hooks/useUnreadNotifications.ts` → only token imports, no literals (shadowColor '#000' exception per repo convention)
</verification>

<success_criteria>
- Inbox + unread badge + read sync working against the 06-08 API; preferences UI writes every schema-backed control
- NOTIF-04 UI half + NOTIF-05 complete pending device verification (06-10)
</success_criteria>

<output>
After completion, create `.planning/phases/06-notification-orchestration/06-09-SUMMARY.md`
</output>
