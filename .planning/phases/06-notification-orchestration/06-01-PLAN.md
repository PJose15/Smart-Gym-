---
phase: 06-notification-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/index.ts
  - apps/mobile/src/lib/notificationService.ts
  - apps/mobile/src/lib/__tests__/notificationService.test.ts
autonomous: true
requirements: [NOTIF-03]
must_haves:
  truths:
    - "NotificationType union has all 24 values (4 existing preserved unchanged)"
    - "NOTIFICATION_ROUTES maps every one of the 24 types to a mobile route — tsc Record totality proves it"
    - "Tapping any typed push navigates to an existing screen (challenges/[id], program, coach-notes/[id], leaderboard, feed, progress, profile, home)"
    - "The route resolver is exported so the Phase 6 inbox screen can reuse it for tap-navigation"
  artifacts:
    - path: "packages/types/src/index.ts"
      provides: "24-value NotificationType union + full NotificationPreferences interface matching migration 001/021 columns"
      contains: "pr_achieved"
    - path: "apps/mobile/src/lib/notificationService.ts"
      provides: "NOTIFICATION_ROUTES for all 24 types + exported resolveNotificationRoute(type, data)"
      exports: ["resolveNotificationRoute"]
    - path: "apps/mobile/src/lib/__tests__/notificationService.test.ts"
      provides: "Route-map totality test (24 keys, every value returns a '/'-prefixed string)"
      min_lines: 30
  key_links:
    - from: "packages/types/src/index.ts"
      to: "apps/mobile/src/lib/notificationService.ts"
      via: "Record<NotificationType, ...> totality — expanding the union without the map breaks mobile tsc"
      pattern: "Record<\\s*NotificationType"
---

<objective>
Expand the notification type system from 4 to 24 types, in lockstep: `NotificationType` union in `@nexera/types` + `NOTIFICATION_ROUTES` deep-link map in mobile `notificationService.ts`. These MUST change together — `NOTIFICATION_ROUTES` is typed `Record<NotificationType, ...>`, so workspace tsc gates the pairing.

Purpose: Every other Phase 6 plan consumes these types. The dispatcher (06-03) uses the union for its category map; trigger wiring (06-05/06/07) passes typed values; the inbox screen (06-09) reuses the route resolver.
Output: expanded types package, expanded route map, mobile route-map test.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-notification-orchestration/06-RESEARCH.md
@packages/types/src/index.ts
@apps/mobile/src/lib/notificationService.ts
</context>

<interfaces>
Current union (packages/types/src/index.ts lines 752-756) — the 4 existing values MUST be preserved verbatim:
```typescript
export type NotificationType =
  | 'coach_note'
  | 'badge_unlocked'
  | 'streak_milestone'
  | 'leaderboard_rank';
```

Current `NotificationPreferences` interface only has `{ id, profile_id, enabled, created_at, updated_at }` — WRONG vs actual schema. Real table (migration 001 + 021): keyed by `member_id` (members.id, NOT profile_id), with per-category push booleans `push_prs, push_achievements, push_level_up, push_challenge_rank, push_new_program, push_trainer_note, push_gym_feed`, quiet hours `quiet_hours_enabled, quiet_hours_start (time), quiet_hours_end (time)`, plus `enabled` (added migration 021), `updated_at`.

Current NOTIFICATION_ROUTES (apps/mobile/src/lib/notificationService.ts lines 119-130) — keep the 4 existing entries' behavior identical.

THE CANONICAL 24-TYPE LIST (all plans reference this — do not deviate):
```typescript
export type NotificationType =
  // Activity
  | 'pr_achieved' | 'badge_unlocked' | 'level_up' | 'streak_milestone'
  | 'streak_broken' | 'leaderboard_rank' | 'challenge_rank_change' | 'challenge_complete'
  // Social
  | 'feed_reaction' | 'feed_comment' | 'new_follower'
  // Coaching
  | 'coach_note' | 'checkin_generated' | 'checkin_reply' | 'program_assigned'
  // Operational (owner/trainer-facing)
  | 'trial_ending' | 'payment_failed' | 'subscription_cancelled'
  | 'member_at_risk' | 'weekly_summary' | 'checkin_overdue' | 'machine_underutilized'
  // Agent-initiated (member-facing, is_agent_initiated=true)
  | 'agent_dormant_alert' | 'agent_welcome';
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Expand NotificationType union + fix NotificationPreferences interface</name>
  <files>packages/types/src/index.ts</files>
  <action>
In the "Push Notifications" section of packages/types/src/index.ts:

1. Replace the 4-value `NotificationType` union with the canonical 24-value list from the interfaces block above (keep the category comments — they document intent).
2. Rewrite `NotificationPreferences` to match the actual DB schema: `{ id: string; member_id: string; enabled: boolean; push_prs: boolean; push_achievements: boolean; push_level_up: boolean; push_challenge_rank: boolean; push_new_program: boolean; push_trainer_note: boolean; push_gym_feed: boolean; quiet_hours_enabled: boolean; quiet_hours_start: string; quiet_hours_end: string; updated_at: string }`. (Postgres `time` arrives as 'HH:MM:SS' string.) Do NOT include the sms_*/email_* columns — out of scope for v1.0 push work; note that in a comment.
3. Update `NotificationLog['status']` union to `'sent' | 'failed' | 'delivered'` (already correct — verify only; migration 031 in plan 06-02 aligns the DB CHECK).

Then check for union-widening ripples: run workspace typecheck for BOTH apps. `apps/mobile/app/settings.tsx` reads only `enabled` via untyped supabase rows — expected to compile unchanged. If any web-admin/mobile file has an exhaustive switch or Record over NotificationType (other than NOTIFICATION_ROUTES, handled in Task 2), fix it in the same commit.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx tsc --noEmit</automated>
  </verify>
  <done>Union has 24 values, 4 originals preserved verbatim; NotificationPreferences matches migration 001/021 columns keyed by member_id; web-admin tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand NOTIFICATION_ROUTES + export resolveNotificationRoute + totality test</name>
  <files>apps/mobile/src/lib/notificationService.ts, apps/mobile/src/lib/__tests__/notificationService.test.ts</files>
  <behavior>
    - Test 1: NOTIFICATION_ROUTES (via resolveNotificationRoute) handles all 24 NotificationType values — enumerate the 24 in the test as a literal array; for each, resolveNotificationRoute(type, {}) returns a string starting with '/'
    - Test 2: coach_note with { note_id: 'abc' } → '/coach-notes/abc'; with {} → '/(tabs)/profile' (existing behavior preserved)
    - Test 3: challenge_rank_change and challenge_complete with { challenge_id: 'xyz' } → '/challenges/xyz'; with {} → '/(tabs)/feed'
    - Test 4: program_assigned → '/program'; leaderboard_rank → '/leaderboard' (existing behavior preserved)
    - Mock expo-notifications, expo-device, expo-constants, expo-router, ../supabase, ../featureFlags, ../events with explicit jest.mock factories (follow existing mobile test conventions in apps/mobile/src/lib/__tests__/)
  </behavior>
  <action>
Write the test FIRST (RED), then implement:

1. In notificationService.ts, expand NOTIFICATION_ROUTES to cover all 24 types. Keep the 4 existing entries byte-identical in behavior. New mappings:
   - pr_achieved → '/(tabs)/progress'
   - level_up, streak_milestone, badge_unlocked → '/(tabs)/profile'
   - streak_broken, agent_dormant_alert, agent_welcome → home tab (verify the home tab path by grepping existing `router.push` calls to the home tab in apps/mobile; likely '/(tabs)')
   - challenge_rank_change, challenge_complete → (d) => d.challenge_id ? `/challenges/${d.challenge_id}` : '/(tabs)/feed'
   - feed_reaction, feed_comment, new_follower → '/(tabs)/feed'
   - checkin_generated, checkin_reply → '/coach-notes'
   - program_assigned → '/program'
   - trial_ending, payment_failed, subscription_cancelled, member_at_risk, weekly_summary, checkin_overdue, machine_underutilized → '/(tabs)/profile' (owner-facing; safe fallback on the member app)
2. Export `resolveNotificationRoute(type: NotificationType, data: Record<string, string>): string | null` — returns NOTIFICATION_ROUTES[type]?.(data) ?? null. Refactor `handleNotificationResponse` to use it. The Phase 6 inbox screen (plan 06-09) reuses this export for tap-navigation.
3. Deep-link safety: challenges/[id] and coach-notes/[id] already render graceful "not found" states (Phase 3 verified) — no changes needed there; note it in a comment.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx jest notificationService --silent</automated>
  </verify>
  <done>All 24 types mapped; resolveNotificationRoute exported; mobile tsc clean (Record totality proves pairing); route-map test passes; full mobile suite still green (227+ tests).</done>
</task>

</tasks>

<verification>
- `cd apps/web-admin && npx tsc --noEmit` — clean (union ripple check)
- `cd apps/mobile && npx tsc --noEmit && npx jest --silent` — clean, all suites green
</verification>

<success_criteria>
- NotificationType = exactly the canonical 24 values; NotificationPreferences matches real schema
- NOTIFICATION_ROUTES total over the union; resolveNotificationRoute exported and tested
- Zero regressions: web-admin 426+ and mobile 227+ tests green
</success_criteria>

<output>
After completion, create `.planning/phases/06-notification-orchestration/06-01-SUMMARY.md`
</output>
