---
phase: 06-notification-orchestration
plan: 07
type: execute
wave: 3
depends_on: ["06-01", "06-03"]
files_modified:
  - apps/web-admin/src/app/api/billing/webhook/route.ts
  - apps/web-admin/src/app/api/cron/agent-daily/route.ts
  - apps/web-admin/src/app/api/cron/agent-weekly/route.ts
  - apps/web-admin/src/app/api/members/onboard/route.ts
autonomous: true
requirements: [NOTIF-02]
must_haves:
  truths:
    - "Billing events (trial_ending, payment_failed, subscription_cancelled) push to the gym OWNER's devices via resolveOwnerProfileId — Phase 5 agent triggers untouched alongside"
    - "Agent cron outputs now deliver member/owner pushes with is_agent_initiated: true — the Phase 5 loop-safety flag flows end-to-end and can never re-trigger agents (dispatcher structurally cannot)"
    - "Dormant members get a re-engagement push (agent_dormant_alert); owners get weekly_summary, member_at_risk, checkin_overdue, machine_underutilized pushes"
    - "New members completing onboarding get an agent_welcome push"
    - "member_at_risk pushes contain NO member names (PII) — owner opens the dashboard for details"
  artifacts:
    - path: "apps/web-admin/src/app/api/billing/webhook/route.ts"
      provides: "owner pushes for the 3 billing actions beside existing agent triggers"
      contains: "resolveOwnerProfileId"
    - path: "apps/web-admin/src/app/api/cron/agent-daily/route.ts"
      provides: "agent_dormant_alert (members), checkin_overdue + machine_underutilized (owner), challenge_complete (participants on auto-expiry) — all is_agent_initiated: true"
      contains: "agent_dormant_alert"
    - path: "apps/web-admin/src/app/api/cron/agent-weekly/route.ts"
      provides: "weekly_summary + member_at_risk owner pushes, is_agent_initiated: true"
      contains: "weekly_summary"
  key_links:
    - from: "apps/web-admin/src/app/api/billing/webhook/route.ts"
      to: "apps/web-admin/src/lib/notifications/dispatcher.ts"
      via: "resolveOwnerProfileId(gymId) → sendNotification(profile_id)"
      pattern: "sendNotification"
    - from: "apps/web-admin/src/app/api/cron/agent-daily/route.ts"
      to: "apps/web-admin/src/lib/notifications/dispatcher.ts"
      via: "is_agent_initiated: true on every agent-sourced dispatch"
      pattern: "is_agent_initiated:\\s*true"
---

<objective>
Wire the operational/billing cluster and Phase 5 agent-output delivery: trial_ending, payment_failed, subscription_cancelled (owner), agent_dormant_alert (member), checkin_overdue, machine_underutilized, weekly_summary, member_at_risk (owner), challenge_complete (cron auto-expiry path), agent_welcome (onboarding).

Purpose: completes NOTIF-02's 24-type wiring. This is where Phase 5's agent outputs finally reach devices — every agent-sourced dispatch carries is_agent_initiated: true, and the dispatcher (which has no agent-trigger imports) guarantees no loop.
Output: four wired files; all 24 types now have live emitting paths (06-05: 8 activity, 06-06: 7 coaching/social, here: 9 operational/agent + the cron leg of challenge_complete).
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-notification-orchestration/06-RESEARCH.md
@.planning/phases/06-notification-orchestration/06-03-SUMMARY.md
@apps/web-admin/src/app/api/billing/webhook/route.ts
@apps/web-admin/src/app/api/cron/agent-daily/route.ts
@apps/web-admin/src/app/api/cron/agent-weekly/route.ts
</context>

<interfaces>
Dispatcher owner path: `resolveOwnerProfileId(admin, gymId)` (exported from dispatcher.ts, 06-03) → `sendNotification({ gym_id, profile_id: ownerProfileId, type, title, body, data, is_agent_initiated })`. Owner sends have no member row → dispatcher skips inbox write, dedups via notification_log — expected behavior.

Billing webhook facts: `handleStripeWebhook(event)` returns `{ gymId, action }`; existing switch fires agents for 'subscription_cancelled' | 'payment_failed' | 'trial_ending'. The webhook has no admin Supabase client in the route today — create one via the repo's established service-role pattern, or add the lookup inside the dispatcher call chain (resolveOwnerProfileId needs a client — instantiate the admin client in the route).

agent-daily structure (read in context): dormant scan (batches of 10, retention-agent), checkin SLA (operations-agent), machine underutilization (operations-agent), challenge auto-expiry (growth-agent block near line 191 — verify which block handles expiry when reading). Each scan already has the member/gym data needed for dispatch.

Billing/ops pushes are NOT agent-initiated (Stripe/cron-sourced business events): is_agent_initiated omitted for trial_ending/payment_failed/subscription_cancelled. ALL dispatches inside agent-daily/agent-weekly ARE agent outputs: is_agent_initiated: true.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Billing webhook owner pushes</name>
  <files>apps/web-admin/src/app/api/billing/webhook/route.ts</files>
  <behavior>
    - action 'trial_ending' → sendNotification called once with type 'trial_ending' and the owner's profile_id (mock dispatcher module incl. resolveOwnerProfileId)
    - action 'payment_failed' → type 'payment_failed'; 'subscription_cancelled' → type 'subscription_cancelled'
    - Existing triggerUptimizeAIAgent calls still fire for all three actions (extend the existing webhook test suite if one exists under api/billing/__tests__, else create webhook push tests following its conventions)
    - resolveOwnerProfileId returning null → no dispatch, no throw, webhook still 200
  </behavior>
  <action>
In the existing switch on result.action, beside each agent trigger add a fire-and-forget owner push: resolve owner via resolveOwnerProfileId(admin, gymId) then sendNotification with titles/bodies — trial_ending: 'Trial ending soon' / 'Your Nexera trial ends in 3 days. Choose a plan to keep your gym live.'; payment_failed: 'Payment failed' / 'Your latest payment failed. Update your billing details.'; subscription_cancelled: 'Subscription cancelled' / 'Your subscription was cancelled. You can resubscribe anytime.' Wrap the resolve+send in an async IIFE with .catch so the webhook response is never blocked or failed by push issues.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="webhook" --silent</automated>
  </verify>
  <done>Three billing actions push to owner; agent triggers untouched; webhook resilient to push failures.</done>
</task>

<task type="auto">
  <name>Task 2: Agent cron delivery — daily + weekly, is_agent_initiated true</name>
  <files>apps/web-admin/src/app/api/cron/agent-daily/route.ts, apps/web-admin/src/app/api/cron/agent-weekly/route.ts</files>
  <action>
agent-daily — add fire-and-forget dispatches beside the existing agent triggers (never replacing them):
1. Dormant scan: per dormant member → sendNotification({ gym_id, member_id, type: 'agent_dormant_alert', title: 'We miss you', body: 'It has been a while — your gym is ready when you are.', is_agent_initiated: true }). Reuse the existing batch loop; dispatcher guards handle opt-outs/caps.
2. Check-in SLA block: per affected gym → owner push type 'checkin_overdue', title 'Check-ins overdue', body 'Some member check-ins are past their deadline.', is_agent_initiated: true (resolveOwnerProfileId).
3. Machine underutilization block: owner push type 'machine_underutilized', title 'Machine usage alert', body 'A machine is seeing unusually low usage. See analytics.', is_agent_initiated: true.
4. Challenge auto-expiry block: per expired challenge, fetch participants and dispatch 'challenge_complete' to each (title 'Challenge finished', body 'A challenge you joined has ended — see the final leaderboard', data { challenge_id }, is_agent_initiated: true). The dispatcher's 5-min (member, type) dedup absorbs overlap with the owner-complete path (06-05).
Update the route's returned counters with pushes_dispatched totals (additive — keep existing counters).

agent-weekly — inside the per-gym loop (skip-if-zero-members logic already there):
5. Owner push 'weekly_summary': title 'Your weekly gym summary', body 'Your weekly summary is ready — open your dashboard.', is_agent_initiated: true.
6. At-risk early-warning block: ONE owner push 'member_at_risk' per gym per run (not per member — barrage rule): title 'Members may be at risk', body 'Some members show at-risk patterns. Review retention insights.', is_agent_initiated: true. NO member names in title/body/data (PII).
Existing extensive cron tests: keep green; extend the cron suites only where mocks force it (dispatcher mock returning resolved 'sent').
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="agent-daily|agent-weekly" --silent</automated>
  </verify>
  <done>All cron-sourced pushes dispatch with is_agent_initiated: true; existing agent triggers and counters unchanged; suites green.</done>
</task>

<task type="auto">
  <name>Task 3: agent_welcome on member onboarding</name>
  <files>apps/web-admin/src/app/api/members/onboard/route.ts</files>
  <action>
Read apps/web-admin/src/app/api/members/onboard/route.ts (member onboarding completion — the point where a member becomes active with a linked auth user; if this route turns out to be something else, grep for the route that sets members to active/claimed status from mobile onboarding — Phase 1 claim flow — and wire there instead, documenting the actual site in the SUMMARY). After successful onboarding completion, dispatch fire-and-forget: sendNotification({ gym_id, member_id, type: 'agent_welcome', title: 'Welcome to your gym', body: 'You are all set. Scan any machine to start your first workout.', is_agent_initiated: true }). Note: freshly onboarded members may have no device token yet → 'no_devices' result is expected and harmless.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx tsc --noEmit && npx jest --silent</automated>
  </verify>
  <done>agent_welcome has a live emitting path at onboarding completion; full suite green.</done>
</task>

</tasks>

<verification>
- `cd apps/web-admin && npx jest --silent` — full suite green
- 24-type coverage audit: `grep -rn "type: '" apps/web-admin/src --include="*.ts" | grep -oE "'(pr_achieved|badge_unlocked|level_up|streak_milestone|streak_broken|leaderboard_rank|challenge_rank_change|challenge_complete|feed_reaction|feed_comment|new_follower|coach_note|checkin_generated|checkin_reply|program_assigned|trial_ending|payment_failed|subscription_cancelled|member_at_risk|weekly_summary|checkin_overdue|machine_underutilized|agent_dormant_alert|agent_welcome)'" | sort -u | wc -l` → 24 (every type has at least one emitting call site; leaderboard_rank/streak types emit via sessionPush.ts literals)
</verification>

<success_criteria>
- Owner billing pushes live; all agent cron outputs deliver with is_agent_initiated: true; agent_welcome wired
- NOTIF-02 complete: all 24 types have emitting code paths across 06-05/06/07
</success_criteria>

<output>
After completion, create `.planning/phases/06-notification-orchestration/06-07-SUMMARY.md`
</output>
