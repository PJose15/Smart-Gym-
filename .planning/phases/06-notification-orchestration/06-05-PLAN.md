---
phase: 06-notification-orchestration
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-03"]
files_modified:
  - apps/web-admin/src/app/api/sessions/[sessionId]/complete/route.ts
  - apps/web-admin/src/app/api/sessions/[sessionId]/complete/__tests__/complete-push.test.ts
  - apps/web-admin/src/lib/notifications/sessionPush.ts
  - apps/web-admin/src/lib/notifications/__tests__/sessionPush.test.ts
  - apps/web-admin/src/lib/challengeScoring.ts
  - apps/web-admin/src/app/api/challenges/[challengeId]/complete/route.ts
autonomous: true
requirements: [NOTIF-02]
must_haves:
  truths:
    - "One completed workout produces AT MOST ONE push even when it triggers PR + badge + level-up + streak simultaneously (coalescer picks highest priority; dispatcher caps are backstop)"
    - "A member entering a challenge top-3 gets a challenge_rank_change push deep-linking to that challenge"
    - "Owner-completed challenges push challenge_complete to participants"
    - "No push body contains member names, weights, or exercise names (PII lock-screen rule)"
  artifacts:
    - path: "apps/web-admin/src/lib/notifications/sessionPush.ts"
      provides: "pickSessionPushEvent pure coalescer (priority: level_up > badge_unlocked > pr_achieved > streak_milestone > streak_broken > leaderboard_rank) + sendSessionCompletePush"
      exports: ["pickSessionPushEvent", "sendSessionCompletePush"]
    - path: "apps/web-admin/src/app/api/sessions/[sessionId]/complete/route.ts"
      provides: "single coalesced dispatcher call after achievements/streak logic"
      contains: "sendSessionCompletePush"
    - path: "apps/web-admin/src/lib/challengeScoring.ts"
      provides: "challenge_rank_change dispatch on top-3 entry"
      contains: "challenge_rank_change"
  key_links:
    - from: "apps/web-admin/src/app/api/sessions/[sessionId]/complete/route.ts"
      to: "apps/web-admin/src/lib/notifications/dispatcher.ts"
      via: "sendSessionCompletePush → sendNotification (fire-and-forget .catch)"
      pattern: "sendSessionCompletePush"
    - from: "apps/web-admin/src/app/api/challenges/[challengeId]/complete/route.ts"
      to: "apps/web-admin/src/lib/notifications/dispatcher.ts"
      via: "sendNotification type 'challenge_complete' per participant"
      pattern: "challenge_complete"
---

<objective>
Wire the highest-value trigger cluster: session-complete (pr_achieved, badge_unlocked, level_up, streak_milestone, streak_broken, leaderboard_rank — coalesced to one push) plus challenge events (challenge_rank_change in scoring, challenge_complete on owner completion).

Purpose: NOTIF-02's activity triggers — and the explicit barrage-prevention success criterion: "One workout that triggers multiple events does not produce a barrage."
Output: coalescer helper + wired session route, challenge scoring, challenge complete route.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-notification-orchestration/06-RESEARCH.md
@.planning/phases/06-notification-orchestration/06-03-SUMMARY.md
@apps/web-admin/src/app/api/sessions/[sessionId]/complete/route.ts
@apps/web-admin/src/lib/challengeScoring.ts
@apps/web-admin/src/app/api/challenges/[challengeId]/complete/route.ts
</context>

<interfaces>
Dispatcher (from 06-03 — call, never reimplement):
```typescript
import { sendNotification } from '@/lib/notifications/dispatcher';
// sendNotification({ gym_id, type, title, body, member_id?, profile_id?, data?, is_agent_initiated? }): Promise<SendResult>
// Fire-and-forget at call sites: sendNotification({...}).catch(err => console.error('[site] push failed:', err));
```

Session route facts (read the file — already loaded in context): after `checkAchievementsForMember` it has `achievements.leveledUp`, `achievements.newAchievements` (array), `streak`, `previousStreak`, `session.is_personal_best`, `session.gym_id`, `member_id`. Streak milestones: {7, 14, 30, 50, 100}.

PII rule for ALL bodies in this plan: generic strings only. "New personal record — see your progress", "You reached a new level", "Badge unlocked", "Streak milestone hit", "Your challenge rank changed". NO names/weights/exercise names.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sessionPush coalescer (pure) + tests</name>
  <files>apps/web-admin/src/lib/notifications/sessionPush.ts, apps/web-admin/src/lib/notifications/__tests__/sessionPush.test.ts</files>
  <behavior>
    - pickSessionPushEvent({ leveledUp, badgesUnlocked, isPersonalBest, streak, previousStreak }) returns exactly one { type, title, body, data } or null
    - Priority: level_up > badge_unlocked > pr_achieved > streak_milestone (streak ∈ {7,14,30,50,100}) > streak_broken (previousStreak>1 && streak===1) > null
    - Test: all-true input → level_up only; PR+streak-7 → pr_achieved; nothing → null; streak 8 (not milestone) with no other events → null; previousStreak 5 → streak 1 with no other events → streak_broken
    - When multiple events coalesce, body acknowledges the combo generically ("Big session — PR and more. See your progress") without PII
  </behavior>
  <action>
RED first. Implement pickSessionPushEvent as a pure function (no I/O) and sendSessionCompletePush(admin-agnostic wrapper) that calls pickSessionPushEvent then `sendNotification({ gym_id, member_id, type, title, body, data })` fire-and-forget, returning void. Titles/bodies per PII rule in interfaces block.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest sessionPush --silent</automated>
  </verify>
  <done>Pure coalescer with total priority ordering; wrapper delegates to dispatcher.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire session-complete route to the coalescer</name>
  <files>apps/web-admin/src/app/api/sessions/[sessionId]/complete/route.ts, apps/web-admin/src/app/api/sessions/[sessionId]/complete/__tests__/complete-push.test.ts</files>
  <behavior>
    - Completing a session that leveled up → exactly ONE sendNotification call with type 'level_up' (mock '@/lib/notifications/dispatcher')
    - Completing a plain session (no PR/badge/level/milestone) → zero sendNotification calls
    - Push dispatch failure never fails the route (mock rejects → route still 200)
    - Existing agent triggers (level-up, streak-broken, leaderboard-updated) still fire unchanged — extend/copy conventions from complete-agents.test.ts, keep that suite green
  </behavior>
  <action>
After the existing feed/challenge/agent fire-and-forget block in the session-complete route, add one call: `sendSessionCompletePush({ gym_id: session.gym_id, member_id, leveledUp: achievements.leveledUp, badgesUnlocked: achievements.newAchievements.length, isPersonalBest: session.is_personal_best ?? false, streak, previousStreak })` — fire-and-forget with .catch. Do NOT add per-event sendNotification calls; the coalescer is the barrage guard (dispatcher 5-min dedup + 10/hr cap are backstops). jest.mock note (STATE.md decision): if the mocked module exports Zod schemas use jest.requireActual('zod') in the factory.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="complete" --silent</automated>
  </verify>
  <done>One coalesced push per completed session; route resilience proven; complete-agents suite still green.</done>
</task>

<task type="auto">
  <name>Task 3: Challenge triggers — rank change (scoring) + challenge_complete (owner route)</name>
  <files>apps/web-admin/src/lib/challengeScoring.ts, apps/web-admin/src/app/api/challenges/[challengeId]/complete/route.ts</files>
  <action>
1. challengeScoring.ts (`updateChallengeScores`): after a participant's score update, determine rank entry into top 3 — query current rank (count of participants in the challenge with higher current_value + 1) before and after the update, or reuse rank data if the function already computes it (read the file first). If newRank <= 3 AND oldRank > 3 → `sendNotification({ gym_id, member_id, type: 'challenge_rank_change', title: 'Leaderboard update', body: 'You moved into the top 3 of a challenge', data: { challenge_id } })` fire-and-forget. Keep the extra queries inside the existing per-challenge loop, head-count only.
2. challenges/[challengeId]/complete route (owner completes a challenge): after the completion write, fetch participants (member_id, gym_id from the challenge) and dispatch `challenge_complete` per participant fire-and-forget: title 'Challenge finished', body 'A challenge you joined has ended — see the final leaderboard', data { challenge_id }. Sequential loop is fine at launch scale; the dispatcher's per-member guards handle opt-outs. NOTE: agent-daily auto-expiry gets the same dispatch in plan 06-07 (file ownership) — the dispatcher's 5-min (member, type) dedup absorbs the overlap when both paths fire.
3. No test files for these two beyond tsc + existing suites (scoring internals are covered indirectly; the device checkpoint in 06-10 verifies end-to-end). Keep changes additive — do not alter scoring math or completion semantics.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx tsc --noEmit && npx jest --silent</automated>
  </verify>
  <done>Top-3 entry dispatches challenge_rank_change with challenge_id deep-link data; owner completion dispatches challenge_complete to all participants; full suite green.</done>
</task>

</tasks>

<verification>
- `cd apps/web-admin && npx jest --testPathPattern="sessionPush|complete" --silent` green
- `cd apps/web-admin && npx tsc --noEmit && npx jest --silent` — full suite green
- Grep check: no direct Edge Function fetch added in any touched file (`grep -r "send-push-notification" apps/web-admin/src/app` → only dispatcher)
</verification>

<success_criteria>
- Session complete emits at most one push (coalesced); challenge rank/complete wired with correct deep-link data
- All bodies PII-free; all dispatch is fire-and-forget through the dispatcher
</success_criteria>

<output>
After completion, create `.planning/phases/06-notification-orchestration/06-05-SUMMARY.md`
</output>
