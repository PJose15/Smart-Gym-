---
phase: 06-notification-orchestration
plan: 08
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/web-admin/src/app/api/member/notifications/route.ts
  - apps/web-admin/src/app/api/member/notifications/[notifId]/read/route.ts
  - apps/web-admin/src/app/api/member/notifications/__tests__/notifications.test.ts
autonomous: true
requirements: [NOTIF-05]
must_haves:
  truths:
    - "A member can list their notification history newest-first with cursor pagination and get their unread count in one call"
    - "Marking a notification read sets read_at exactly once and only for the owning member (cross-member reads impossible)"
    - "Both routes enforce verifyMember auth (Bearer JWT falls through to cookie — mobile-compatible, established pattern)"
  artifacts:
    - path: "apps/web-admin/src/app/api/member/notifications/route.ts"
      provides: "GET inbox list: { notifications, unread_count, next_cursor }"
      exports: ["GET"]
    - path: "apps/web-admin/src/app/api/member/notifications/[notifId]/read/route.ts"
      provides: "POST mark-read scoped to member"
      exports: ["POST"]
  key_links:
    - from: "apps/web-admin/src/app/api/member/notifications/route.ts"
      to: "notifications table"
      via: "member_id-scoped select using idx_notifications_member_recent / idx_notifications_member_unread"
      pattern: "from\\('notifications'\\)"
---

<objective>
Build the inbox API: GET /api/member/notifications (paginated list + unread count) and POST /api/member/notifications/[notifId]/read (mark-read). The mobile inbox screen (06-09) consumes both.

Purpose: NOTIF-05 — notification history with unread badge and read-state sync. The `notifications` table, unread indexes (idx_notifications_member_unread, idx_notifications_member_recent), and RLS already exist (migration 001); this is pure route work.
Output: two routes + test suite.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-notification-orchestration/06-RESEARCH.md
@apps/web-admin/src/lib/auth/verifyMember.ts
@apps/web-admin/src/app/api/member/feed/comments/route.ts
</context>

<interfaces>
notifications columns (migration 001): id, member_id, gym_id, notification_type, title, body, data jsonb, channel, status, sent_at, read_at, created_at.

verifyMember pattern (repo standard for member API routes — see feed routes): `const auth = await verifyMember(member_id); if (auth instanceof NextResponse) return auth; const { admin } = auth;` — member_id arrives as query param on GET (`?member_id=`) and in the JSON body on POST, both validated with uuidString/validateUUIDs.

Response contract (06-09 mobile service compiles against this):
```typescript
// GET /api/member/notifications?member_id=&cursor=&limit=
{ notifications: Array<{ id: string; notification_type: string; title: string; body: string;
    data: Record<string, string>; read_at: string | null; created_at: string }>,
  unread_count: number,
  next_cursor: string | null }   // created_at of last row when a full page returned
// POST /api/member/notifications/[notifId]/read  body { member_id }
{ success: true }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inbox GET + mark-read POST (TDD)</name>
  <files>apps/web-admin/src/app/api/member/notifications/route.ts, apps/web-admin/src/app/api/member/notifications/[notifId]/read/route.ts, apps/web-admin/src/app/api/member/notifications/__tests__/notifications.test.ts</files>
  <behavior>
    - GET without valid member_id UUID → 400; verifyMember failure → its NextResponse passthrough
    - GET returns rows ordered created_at DESC, default limit 20 (cap 50), filtered member_id; cursor param adds .lt('created_at', cursor); next_cursor = last row's created_at when page is full, else null
    - GET returns unread_count via head-count query (member_id, read_at IS NULL) — runs in Promise.all with the list query
    - POST marks read: update read_at=now WHERE id=notifId AND member_id=member_id AND read_at IS NULL; already-read → still { success: true } (idempotent); notifId belonging to another member → 404
    - POST invalid UUIDs → 400
  </behavior>
  <action>
RED first (mock @supabase/supabase-js + verifyMember per feed-route test conventions). Implement both routes:
- GET: parse member_id/cursor/limit from searchParams, validateUUIDs on member_id, verifyMember, Promise.all([list query, unread head-count]), map rows to the response contract (select only the contract columns — no SELECT *, repo convention).
- POST: params Promise<{ notifId }> (Next 14 App Router convention used across the repo), validateUUIDs both ids, verifyMember(member_id), scoped update returning select('id'); no row AND a pre-check shows the id exists for the member and is read → idempotent success; id truly absent for member → 404.
- Rate limit: checkRateLimit(`notif-read:${member_id}`, 60, 60_000) on POST (repo pattern).
- Route files export ONLY HTTP handlers (repo convention from Phase 1 decision — helpers into adjacent modules if needed).
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="member.*notifications" --silent</automated>
  </verify>
  <done>Both routes green under tests; response matches the contract exactly; full suite still green.</done>
</task>

</tasks>

<verification>
- `cd apps/web-admin && npx jest --silent && npx tsc --noEmit` — clean
</verification>

<success_criteria>
- Inbox list + unread count + idempotent mark-read, member-scoped, contract-stable for 06-09
</success_criteria>

<output>
After completion, create `.planning/phases/06-notification-orchestration/06-08-SUMMARY.md`
</output>
