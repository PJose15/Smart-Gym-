---
phase: 06-notification-orchestration
plan: 06
type: execute
wave: 3
depends_on: ["06-01", "06-03"]
files_modified:
  - apps/web-admin/src/lib/checkIn/sendCheckIn.ts
  - apps/web-admin/src/app/api/member/[memberId]/check-ins/[checkInId]/reply/route.ts
  - apps/web-admin/src/app/api/sessions/[sessionId]/trainer-note/route.ts
  - apps/web-admin/src/app/api/trainer/members/[memberId]/program/assign/route.ts
  - apps/web-admin/src/app/api/member/feed/react/route.ts
  - apps/web-admin/src/app/api/member/feed/comments/route.ts
  - apps/web-admin/src/app/api/member/social/follow/route.ts
autonomous: true
requirements: [NOTIF-02]
must_haves:
  truths:
    - "A sent weekly check-in pushes checkin_generated to the member (replacing the raw notifications insert with a dispatcher call)"
    - "A member reply pushes checkin_reply to the trainer WITHOUT the reply text in the push body (PII fix — current code leaks reply text)"
    - "A trainer note pushes coach_note deep-linking to /coach-notes/[id]"
    - "Program assignment pushes program_assigned deep-linking to /program"
    - "Reactions, comments, and follows push to the receiving member (never to the actor; self-actions never push)"
  artifacts:
    - path: "apps/web-admin/src/lib/checkIn/sendCheckIn.ts"
      provides: "dispatcher-routed checkin_generated (manual notifications.insert removed)"
      contains: "checkin_generated"
    - path: "apps/web-admin/src/app/api/member/feed/react/route.ts"
      provides: "feed_reaction push to event owner on new reaction (not on toggle-off)"
      contains: "feed_reaction"
    - path: "apps/web-admin/src/app/api/member/social/follow/route.ts"
      provides: "new_follower push to followed member"
      contains: "new_follower"
  key_links:
    - from: "apps/web-admin/src/lib/checkIn/sendCheckIn.ts"
      to: "apps/web-admin/src/lib/notifications/dispatcher.ts"
      via: "sendNotification (dispatcher now owns the inbox row)"
      pattern: "sendNotification"
    - from: "apps/web-admin/src/app/api/member/feed/comments/route.ts"
      to: "gym_feed_events.member_id"
      via: "event owner lookup before dispatch"
      pattern: "feed_comment"
---

<objective>
Wire the coaching and social trigger clusters through the dispatcher: checkin_generated, checkin_reply, coach_note, program_assigned, feed_reaction, feed_comment, new_follower.

Purpose: NOTIF-02 coaching + social triggers. Also fixes two existing preference-bypass paths (Pitfall 11): sendCheckIn.ts and the reply route insert directly into `notifications` with no preference check — and the reply route currently puts member reply text into a push-visible body (PII).
Output: seven trigger call sites routed through sendNotification.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-notification-orchestration/06-RESEARCH.md
@.planning/phases/06-notification-orchestration/06-03-SUMMARY.md
@apps/web-admin/src/lib/checkIn/sendCheckIn.ts
@apps/web-admin/src/app/api/member/[memberId]/check-ins/[checkInId]/reply/route.ts
@apps/web-admin/src/app/api/member/feed/react/route.ts
@apps/web-admin/src/app/api/member/feed/comments/route.ts
</context>

<interfaces>
Dispatcher: `sendNotification({ gym_id, type, title, body, member_id?, profile_id?, data? })` — fire-and-forget with .catch at every site. THE DISPATCHER WRITES THE INBOX ROW ITSELF (notifications table, channel 'push') — call sites that currently insert into `notifications` manually MUST remove that insert when switching to the dispatcher, or members get duplicate inbox entries.

Existing manual inserts to replace (both use notification_type 'coach_note' today):
1. sendCheckIn.ts lines 22-35: inserts { member_id, gym_id, 'coach_note', 'Your weekly check-in is ready', ..., data: { check_in_id, url } }
2. reply route lines ~79-102: resolves trainer's member row via `members WHERE user_id = updated.trainer_id AND gym_id`, then inserts with body = replyText.slice(0,100) — PII in a push-bound body; must become generic.

Sites that only ADD a dispatch (no insert to remove): trainer-note route, program assign route, feed react, feed comments, social follow.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coaching triggers — sendCheckIn, check-in reply, trainer-note</name>
  <files>apps/web-admin/src/lib/checkIn/sendCheckIn.ts, apps/web-admin/src/app/api/member/[memberId]/check-ins/[checkInId]/reply/route.ts, apps/web-admin/src/app/api/sessions/[sessionId]/trainer-note/route.ts</files>
  <behavior>
    - sendCheckInToMember: marks sent_at (unchanged) then calls sendNotification with type 'checkin_generated', data { check_in_id }; NO direct notifications.insert remains in the file (mock dispatcher, assert insert never called on 'notifications')
    - reply route: on successful reply, sendNotification type 'checkin_reply' targeted at the trainer's member_id; body is a constant generic string containing NO reply text; direct notifications.insert removed
    - trainer-note route: after note creation, sendNotification type 'coach_note' to the member with data { note_id } (deep-links /coach-notes/[id]); read the route first to find the created note id variable
    - All dispatches fire-and-forget; a rejecting dispatcher never breaks the 200 path
  </behavior>
  <action>
Write/extend tests RED-first (new test files under each route's __tests__ dir, or extend existing suites if present — check first). Then:
1. sendCheckIn.ts: replace the notifications.insert block with `await sendNotification({ gym_id: gymId, member_id: memberId, type: 'checkin_generated', title: 'Your weekly check-in is ready', body: 'Your Nexera Coach reviewed your week. Tap to read.', data: { check_in_id: checkInId } })` — keep it awaited-with-catch or fire-and-forget consistent with the function's current contract (it's already async/awaited by callers; awaiting the dispatcher is fine since it never throws).
2. Reply route: replace the trainer notification insert with sendNotification({ gym_id: updated.gym_id, member_id: trainerMember.id, type: 'checkin_reply', title: 'Check-in reply', body: 'A member replied to their weekly check-in.', data: { check_in_id: checkInId } }). Keep the trainerMember lookup. Fire-and-forget.
3. trainer-note route: read it; after successful note insert, dispatch coach_note to the session's member with data { note_id: createdNote.id }. Body: 'Your trainer left you a note.' Fire-and-forget.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --testPathPattern="checkIn|check-ins|trainer-note" --silent</automated>
  </verify>
  <done>Three coaching triggers dispatch through the dispatcher; zero direct notifications.insert remains in these files; reply push body contains no member text.</done>
</task>

<task type="auto">
  <name>Task 2: Program-assigned + social triggers — assign, react, comment, follow</name>
  <files>apps/web-admin/src/app/api/trainer/members/[memberId]/program/assign/route.ts, apps/web-admin/src/app/api/member/feed/react/route.ts, apps/web-admin/src/app/api/member/feed/comments/route.ts, apps/web-admin/src/app/api/member/social/follow/route.ts</files>
  <action>
1. Program assign route: after the member_program_assignments insert succeeds, dispatch { gym_id, member_id: memberId, type: 'program_assigned', title: 'New training program', body: 'Your trainer assigned you a new program.', data: { program_id: newProgram.id } }. Fire-and-forget.
2. Feed react route: ONLY on the add path (toggled: true, including the 23505 race branch? NO — dispatch only on the clean insert success branch to avoid double-fire). Fetch the event owner: `gym_feed_events` select member_id, gym_id by event_id. Skip when owner member_id === reacting member_id (self-reaction). Dispatch { gym_id, member_id: ownerMemberId, type: 'feed_reaction', title: 'New reaction', body: 'Someone reacted to your activity.', data: { event_id } }. Fire-and-forget; dispatcher's 5-min (member, type) dedup prevents reaction storms.
3. Feed comments route (POST): same owner lookup + self-skip; type 'feed_comment', body 'Someone commented on your activity.', data { event_id }.
4. Social follow route: read it first (apps/web-admin/src/app/api/member/social/follow/route.ts) to find the followed member variables; on successful follow insert, dispatch { gym_id, member_id: followedMemberId, type: 'new_follower', title: 'New follower', body: 'Someone at your gym started following you.', data: {} }. Skip if the route models self-follow (it should already reject it).
PII rule everywhere: no actor names in bodies ("Someone..." phrasing) — names appear only after tap, in-app.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx tsc --noEmit && npx jest --silent</automated>
  </verify>
  <done>Four sites dispatch with correct target member (receiver, never actor), self-actions skipped, full suite green.</done>
</task>

</tasks>

<verification>
- `cd apps/web-admin && npx jest --silent` — full suite green (426+)
- `grep -rn "from('notifications').insert" apps/web-admin/src/lib/checkIn apps/web-admin/src/app/api/member/[memberId]/check-ins` → zero matches (dispatcher owns inbox writes)
</verification>

<success_criteria>
- All 7 coaching/social types have live emitting code paths through the dispatcher
- Preference-bypass inserts eliminated; reply-text PII leak fixed
</success_criteria>

<output>
After completion, create `.planning/phases/06-notification-orchestration/06-06-SUMMARY.md`
</output>
