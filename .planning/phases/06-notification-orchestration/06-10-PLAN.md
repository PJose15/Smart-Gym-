---
phase: 06-notification-orchestration
plan: 10
type: execute
wave: 4
depends_on: ["06-04", "06-05", "06-06", "06-07", "06-09"]
files_modified: []
autonomous: false
requirements: [NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05, NOTIF-06]
must_haves:
  truths:
    - "Full automated gate green: web-admin tsc+jest, mobile tsc+jest, no direct Edge Function calls outside the dispatcher, all 24 types have emitting call sites, zero hardcoded hex in new mobile files"
    - "On a physical device: a completed workout produces exactly one push within a minute whose tap lands on the right screen"
    - "Category opt-out and quiet hours provably silence pushes; inbox lists history and the bell badge syncs on read"
  artifacts: []
  key_links: []
---

<objective>
Phase 6 gate: full automated verification of the notification system, then a human device walkthrough against the Iron Society demo environment.

Purpose: NOTIF-01..06 success criteria are behavioral (push arrives < 1 min, correct deep link, provable silence when opted out) — the automated suite proves logic; the device proves delivery.
Output: gate results + human sign-off; phase completion.
</objective>

<execution_context>
@C:/Users/pjaco/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pjaco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-notification-orchestration/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated gate</name>
  <files>none (verification only)</files>
  <action>
Run and record every gate check; ALL must pass before the checkpoint:

1. `cd apps/web-admin && npx tsc --noEmit && npx jest --silent` — 426+ tests green (dispatcher, sessionPush, receipt-poll, inbox API, trigger suites included)
2. `cd apps/mobile && npx tsc --noEmit && npx jest --silent` — 227+ tests green (notificationService route map, inbox service included)
3. Dispatcher gateway audit: `grep -rn "functions/v1/send-push-notification" apps/web-admin/src --include="*.ts"` → matches ONLY in lib/notifications/dispatcher.ts (+ its tests)
4. Loop-safety audit: `grep -n "triggerUptimizeAIAgent\|billing/triggerAgent" apps/web-admin/src/lib/notifications/dispatcher.ts` → zero matches
5. 24-type emitting-path audit (command from 06-07 verification block) → 24 distinct types found at call sites
6. Hex scan on Phase 6 mobile files: `grep -nE "#[0-9a-fA-F]{3,8}" apps/mobile/app/notifications/index.tsx` → no color literals
7. `cd apps/web-admin && npx eslint src/lib/notifications src/app/api/member/notifications src/app/api/cron/receipt-poll` → clean

If any check fails: fix within this plan only if it's a gate-level integration issue (wiring, import, type ripple); route logic regressions go back as gap-closure plans.
  </action>
  <verify>
    <automated>cd apps/web-admin && npx jest --silent && npx tsc --noEmit</automated>
  </verify>
  <done>All 7 checks pass and are recorded in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Device walkthrough — Iron Society staging</name>
  <files>none (verification only)</files>
  <action>
Human verification on a physical device. Claude pauses here after presenting the walkthrough steps; do not mark NOTIF requirements complete until the user signs off.
  </action>
  <what-built>Complete notification orchestration: 24-type dispatcher with preference/quiet-hours/dedup/rate-cap enforcement, trigger wiring across session/coaching/social/billing/agent sources, mobile inbox + unread badge + preferences UI, receipt-polling cron with admin delivery-rate visibility.</what-built>
  <how-to-verify>
Using the Iron Society demo gym, a claimed member account on a physical device (registered push token — check device_tokens has your active row), and prod-like env (Edge Function reachable):

1. PUSH + DEEP LINK (NOTIF-01/02/03): Complete a workout that sets a PR. Expect exactly ONE push within 60s (coalesced — no barrage even though PR + points fire together). Tap it → app lands on the mapped screen (progress/profile).
2. COACHING: Have a trainer send a check-in (web admin) → 'Your weekly check-in is ready' push arrives; tap → coach-notes.
3. CATEGORY OPT-OUT (NOTIF-04): In mobile settings, turn OFF the matching category (e.g. PRs). Repeat a qualifying event → NO push arrives. The inbox still shows the item (by design — inbox is history, pushes are opt-out).
4. QUIET HOURS (NOTIF-04): Enable quiet hours covering current UTC time. Trigger an enabled-category event → NO push. Disable → pushes resume.
5. INBOX + BADGE (NOTIF-05): Home-header bell shows unread badge. Open /notifications → history newest-first, unread styled. Tap an item → marked read, badge decrements, deep link fires.
6. DELIVERY OPS (NOTIF-06): In Supabase: `SELECT status, count(*) FROM notification_log WHERE created_at > now() - interval '1 day' GROUP BY 1;` shows sent/delivered rows after a receipt-poll cycle (cron runs every 15 min; or POST /api/cron/receipt-poll with x-smartgym-internal-key manually). GET /api/admin/health as super admin → notifications block with delivery_rate.
7. AGENT LOOP SPOT-CHECK: after the above, `SELECT count(*) FROM smartgym_agent_logs WHERE executed_at > now() - interval '1 hour';` — count consistent with events fired, no runaway growth.
  </how-to-verify>
  <verify>Manual: all 7 walkthrough steps confirmed on device + DB spot-checks.</verify>
  <done>User signs off the 7-step walkthrough; Phase 6 requirements NOTIF-01..06 verified end-to-end.</done>
  <resume-signal>Type "approved" to complete Phase 6, or describe which step failed</resume-signal>
</task>

</tasks>

<verification>
- Automated gate: all 7 checks recorded green
- Human sign-off on the 7-step device walkthrough
</verification>

<success_criteria>
- Both suites + tsc green across the workspace; dispatcher is the single push gateway; 24 types wired
- Device walkthrough approved → Phase 6 complete, ROADMAP/REQUIREMENTS traceability updated to Complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-notification-orchestration/06-10-SUMMARY.md`
</output>
