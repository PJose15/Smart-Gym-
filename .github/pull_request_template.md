## What
(one-line summary)

## Why
(motivation / ticket link)

## Changes
- bullet list of changes

## Checklist
- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm -F @nexera/ai-assist test -- --run` passes
- [ ] `pnpm build:web` succeeds
- [ ] No new `any` types introduced
- [ ] Env vars added to `.env.example` if needed
